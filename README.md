# ShopFlow WMS

> Multi-channel warehouse management system for SEA marketplaces, with database-per-tenant hard isolation under PDPA SEA compliance. 12-week single-developer portfolio build.

[![Stage](https://img.shields.io/badge/stage-Sprint--9.5%20Notification%20%2B%20Frontend%20Auth%20UX%20%E2%9C%85-brightgreen)](docs/phase-gates/2026-05-21-sprint-9.5-signoff.md)
[![License](https://img.shields.io/badge/license-TBD-lightgrey)](#license)

**Current stage**: **Sprint-9.5 Notification module + frontend auth UX + cross-tenant integration tests complete (2026-05-21)** — tagged `v0.13.0-sprint-9.5`. Lights up the six units Sprint-9 explicitly deferred. The four Sprint-9 cross-module Auth events (`PasswordResetRequestedV1` / `RefreshReuseDetectedV1` / `AccountLockedV1` / `MfaEnrolledV1`) that were idle in RabbitMQ now flow through the new **`ShopFlow.Notification` module** (7th business module — the canonical quartet shape per AGENTS.md §11.76) → `notification_outbox` per-tenant queue → `NotificationDeliveryDispatcher` background service (multiplex-per-tenant via `ITenantCatalog.GetReadyTenantsAsync` + `FOR UPDATE SKIP LOCKED` claim) → SMTP via `IMailerProvider` (Mailpit in dev / MailKit in prod) → email delivery (KTD2 two-stage decouples broker throughput from SMTP latency). KTD3 idempotency anchor: `UNIQUE(source_event_id, recipient_email)` on `notification_log` — duplicate MT redelivery races a second outbox row but the dispatcher's INSERT into `notification_log` fails on Npgsql SQLState 23505; the duplicate outbox row is silently dropped at debug log level (no double-send). `MailKitSmtpMailer` returns `Result<MessageId>` with stable `mailer.transient.*` / `mailer.permanent.*` error codes per KTD4; transient → retry with backoff up to MaxAttempts (3 by default); permanent OR cap-reached → `notification_dead_letter` row with JSON payload + outbox row deleted. Mailpit pinned to `axllent/mailpit:v1.21.0` per KTD7 (AGENTS.md rule 56 — no `:latest`). **Frontend useAuth 4-state machine** (`signed-out` / `mfa-challenge` / `mfa-enrollment` / `full-session`) with in-memory `intentToken` (KTD8 — never localStorage; 5-min HMAC TTL forces fresh login on reload) + `user.perm: readonly string[]` extracted via `permsFrom()` defensive parse (KTD1+KTD12 fail-closed). httpClient KTD9 401-branches on authState: full-session → single-flight refresh + retry (Sprint-8 inflight-guard preserved); MFA-state → clearIntent + redirect /login; signed-out → redirect /login. 403 surfaces as `ApiError` with `errorCode` from problem-details body — never refresh, never clear session. LoginScreen switches on Sprint-9 LoginResponse's discriminated-union (`{kind: 'success' | 'mfa-challenge' | 'mfa-enrollment' | 'failure'}`). **5 new auth screens** at Sprint-6/7 design idioms: `/forgot-password` (R6 enumeration-discipline — always shows the same success state), `/reset-password` (validateSearch token + 12-char + 4-category-mix password rules), `/mfa/enroll` (3-step stepper: beginEnroll QR + verifyEnroll OTP + RecoveryCodesDisplay), `/mfa/challenge` (autoFocus 6-digit + "Use recovery code instead" toggle + 3-bad-OTP → clearIntent + onSessionExpired), `/_auth/profile/security` (Enable/Disable MFA + Regenerate recovery codes + Change password). **`RecoveryCodesDisplay`** primitive: 10-code monospace grid + Download as .txt (Blob URL revoked in finally — codes don't remain reachable) + mandatory ack-then-Continue. **Owner admin surface** — `/admin/users` (MFA badge column + per-row Reset MFA + Unlock actions with confirmation modal) + `/admin/locked-accounts` (auto-ticking remaining cell + Unlock + empty state) + `/admin/role-permissions` (3-column Owner / Picker / Dispatcher editor with KTD11 Owner-locked column + KTD13-aware diff-confirmation Save flow, module-grouped collapsible rows, no optimistic updates per R27). **Permission-based UI gating** (Sidebar `permRequired` field-driven visibility + `requirePermission()` route-guard helper consumed by all 3 admin routes; KTD10 — frontend reads JWT `perm[]` ahead of Sprint-10+'s backend `[Authorize(Policy=...)]` migration). **AuthCrossTenantTests** (5 scenarios per R32 — same-tenant auth, cross-tenant 401 rejection, role-perm edit isolation, user-list isolation, MFA-reset target isolation) + **4 KTD-pinning integration test classes** (12 scenarios total — JwtPermClaimIntegrationTests for KTD1 wire shape, ChainAwareRefreshIntegrationTests for KTD2 chain-only revoke, TotpEnrollmentIntegrationTests for KTD10 Redis TTL + KTD8 KEK rotation, RolePermissionsOwnerCriticalIntegrationTests for KTD13 server-side guard) — all Skip-marked locally per Sprint-1+ posture; CI runs the full Docker-backed suite. **11 implementation units shipped** (U0-U10) across 11 commits. **Verification gates**: `dotnet build ShopFlow.sln` returns **0 errors + 0 warnings** across 47 projects (41 → 47: +4 Notification quartet + 1 Notification.UnitTests + 1 Notification.IntegrationTests); Notification.UnitTests **66 passed** (smoke + value objects + 4 mailer/template/mapper + 4 MT consumer harnesses via TestHarness + NSubstitute); frontend Vitest **474 passing / 4 pre-existing Sprint-7 baseline failures unchanged** (a11y.smoke / OrderLineItems / useOrderMutations / AdjustStockModal). **Sprint-9.5 KTDs**: 12 captured (KTD1 perm-array shape + KTD2 chain-only revoke + KTD3 UNIQUE on log + KTD4 stable error codes + KTD5 plaintext stays in Auth + KTD6 literal template renderer + KTD7 Mailpit pinned + KTD8 in-memory intentToken + KTD9 httpClient 401 branches + KTD10 UI-gating-ahead-of-policy + KTD11 Owner column read-only + KTD12 usePerm fails closed) + Sprint-9 KTD16 restated for U6 QR `Cache-Control: no-store` reference. **Sprint-9.5 trade-offs carried forward**: U6 a11y harness extension (9 cases) → Sprint-9.6; per-route + per-action-button `usePerm` application across Inventory / Orders / Inbound → Sprint-10+ alongside backend `[Authorize(Policy=...)]` migration; English-only email templates → bilingual waits for `users.preferred_locale` (Sprint-10+); U9 integration test bodies → CI-tier Docker fixture implementation; `ProfileSecurityScreen.mfaEnrolled` stubbed → Sprint-10+ `useMe()` cache; `/admin/users` single-shot fetch → Sprint-10+ TanStack Query. See [Sprint-9.5 sign-off](docs/phase-gates/2026-05-21-sprint-9.5-signoff.md). **Previous**: [Sprint-9 sign-off](docs/phase-gates/2026-05-20-sprint-9-signoff.md) (`v0.12.0-sprint-9`). **Next**: Sprint-10 (per-permission `[Authorize(Policy=...)]` migration of business module controllers), Sprint-9.6 polish (a11y harness extension + per-button perm gating across Inventory/Orders/Inbound + ProfileSecurityScreen useMe() migration), or Phase-3 polish (observability dashboards for notification_outbox depth + dead_letter count + auth-failures-per-tenant).

---

**Sprint-9 Backend Auth Hardening complete (2026-05-20)** — tagged `v0.12.0-sprint-9`. Ships the backend foundation for per-permission RBAC (`PermissionKeys` catalog + JSON-array `perm` claim per KTD1 + 25 ASP.NET policies registered via `AddShopFlowPermissionPolicies`), TOTP MFA (Otp.NET ±1-step drift + AES-256-GCM at-rest with KEK rotation slot per KTD8 + 10 single-use Argon2-hashed recovery codes via the lighter `RecoveryCode` profile per KTD9 + Owner-forced enrollment per R17), per-account lockout (5/15-min sliding window via `User.RegisterFailedLogin` + lockout-boundary `AccountLockedV1` emit) + per-IP rate limit (ASP.NET RateLimiter behind `UseForwardedHeaders` with non-Development startup gate per KTD7), chain-aware refresh-token reuse detection (per-login `chain_id` propagation + 7-day tombstone TTL + code-level grace check per KTD2 + KTD3 + RFC 9700 §4.14) and `RevokeChainAsync` chain-only revoke, self-service password reset (predicate-in-UPDATE token consume + always-200 R6 enumeration discipline + synthetic Argon2 constant-time per KTD14), 4 new cross-module Auth events (`PasswordResetRequestedV1` / `RefreshReuseDetectedV1` / `AccountLockedV1` / `MfaEnrolledV1` published via the new `auth_outbox_messages` table + `MultiplexedOutboxDispatcher<AuthDbContext>`), 7 new public Auth.Api endpoints + 4 new admin endpoints, `RolePermissionsCommandHandler` with `OwnerCritical` server-side guard per KTD13, `HmacMfaChallengeTokenCodec` 5-min compact tokens bridging login → MFA verify, full `users` table extension (5 new columns: `failed_login_count` + `locked_until` + `last_failed_login_at` + `mfa_required` + `mfa_enrolled`), 5 new domain entities + 5 new repositories + 5 new EF entity configurations + 1 consolidated migration (6 new tables + 5 column adds + 2 partial indexes + 1 CHECK constraint + 1 idempotent `RolePermissionsSeed`). **11 of 17 plan units shipped** (U0-U9 + U12-partial + U16-light + U17); U10/U11 (Notification module + Mailpit + 4 consumers + 4 templates) + U13/U14/U15 (frontend useAuth/httpClient split + 5 new screens + Owner admin surface) + U16-extended (cross-tenant + KTD-pinning integration tests against Testcontainers Postgres + Redis) deferred to Sprint-9.5. The 4 Sprint-9 contracts publish to RabbitMQ + idle in MT queues until consumers ship; **no message loss**. **Verification gates**: `dotnet build ShopFlow.sln` returns **0 errors + 0 warnings** across 41 projects; `tests/ShopFlow.Auth.UnitTests/` **173 passed** (+57 vs Sprint-8 baseline); `tests/ShopFlow.SharedKernel.UnitTests/` **47 passed** (+4 PermissionPolicyComposition). 4 new `docs/solutions/` entries (`perm-claim-must-be-json-array.md` + `chain-aware-refresh-tombstone-7d.md` + `totp-kek-rotation-via-key-id.md` + `aspnet-ratelimiter-yarp-x-forwarded-for.md`) + 1 new `src/Services/Auth/AGENTS.md` (R6 + 12 KTDs + ADR-0003 invariants pinned). See [Sprint-9 sign-off](docs/phase-gates/2026-05-20-sprint-9-signoff.md). **Previous**: [Sprint-8.5 sign-off](docs/phase-gates/2026-05-20-sprint-8.5-signoff.md) (`v0.11.1-sprint-8.5`). **Next**: Sprint-9.5 (Notification module + frontend + extended integration tests).

---

**Sprint-8.5 Test Sweep + Build-Error Cleanup complete (2026-05-20)** — tagged `v0.11.1-sprint-8.5`. Closes the Sprint-8 sign-off's recommended next step: restored the 2 frontend Vitest files Sprint-8 U11 deleted (httpClient + LoginScreen — now covering the 401 → refresh interceptor + token-pair + subdomain-detect contracts), fixed 10 pre-existing .NET 9 compile errors across 5 csprojs that blocked `dotnet build ShopFlow.sln`, added 1 OwnerSeed real-Postgres integration test (closes Sprint-8 U10 deferral via a new `tests/ShopFlow.Migrate.IntegrationTests/` project mirroring the Sprint-8 U3 AuthTenantFixture shape), and captured 4 new `docs/solutions/` entries (Polly v8 PredicateBuilder, Microsoft.Extensions.TimeProvider.Testing CPM choice, SkuDimensions using-directive discipline, cross-module contract-evolution consumer-test sweep). 12 implementation units shipped on `feat/sprint-8.5-test-sweep-buildfix` from `v0.11.0-sprint-8`. **Verification gates: `dotnet build ShopFlow.sln` returns 0 errors + 0 warnings; frontend Vitest 394 passing / 3 pre-existing Sprint-7 a11y failures (out of scope, +24 vs Sprint-8 baseline)**. KTD bump: Microsoft.Extensions.Logging pinned 10.0.7 (was 9.0.0 — NU1109 against Hosting 10.0.7) — surfaced 2 latent StockSync.Infrastructure errors that were bundled into U4 (Channel.CreateBounded namespace ambiguity + Npgsql metadata using). Polly v8 PredicateBuilder fix: when the consumer ctor takes the non-generic ResiliencePipeline, the typed PredicateBuilder<T> can't compose — hand-rolled the predicate as a switch expression on args.Outcome (both shapes documented in the solutions note). See [Sprint-8.5 sign-off](docs/phase-gates/2026-05-20-sprint-8.5-signoff.md). **Previous**: [Sprint-8 sign-off](docs/phase-gates/2026-05-20-sprint-8-signoff.md) (`v0.11.0-sprint-8`). **Next**: Sprint-9 (RBAC + MFA hardening), Phase-3 polish (observability dashboards + rate limiting + audit log table), or net-new frontend coverage for Sprint-8 U11 surfaces (UserRow / AuthAdminController / detectTenantFromHost direct unit tests).

---

**Sprint-8 Real Auth Module complete (2026-05-20)** — tagged `v0.11.0-sprint-8`. Retires the Sprint-6 dev-mode baked JWT in favor of production-grade authentication: Argon2id (Konscious) password hashing with OWASP 2026 parameters baked into the PHC, JWT access tokens (HS256, 15-min TTL) issued by the Auth module + validated by the kernel JwtBearer (shared `Auth:DevSecret` — KTD5 closes the doc-review SEC-001/F-001 total-auth-bypass risk), opaque 32-byte refresh tokens in Redis with 60-sec grace-window tombstone rotation (OWASP refined pattern — legitimate concurrent retries within the window return the SAME successor token, no false-positive lockout). Per-tenant `users` table with `ux_users_email_lower` UNIQUE expression-index + `chk_users_role` CHECK constraint + partial `ix_users_role_active` for the Owner-only admin listing. R6 enumeration prevention: every credential-failure leg (missing user / inactive user / wrong password / unknown tenant / malformed PHC hash) collapses to canonical `auth.invalid_credentials` + 401. Owner-only admin surface (`/api/auth/admin/users`) with consolidated `UpdateUserCommand` discriminator (KTD8 — SetRole / ResetPassword / Deactivate route through one handler). Subdomain-first tenant resolution at the controller layer with host-suffix allowlist (SEC-004 hard requirement) + `ReservedSlugs` denylist (ADV-001). Frontend lifts from single-JWT to token-pair model with transparent 401 → refresh interceptor, rememberMe checkbox, subdomain-detect, Sidebar Logout. shopflow-migrate provisions one Owner per tenant with a one-time stdout temp password; new `seed-owner <slug>` subcommand retrofits existing tenants (ADV-003 — operators MUST run before deploying Sprint-8 to legacy fleets). 12 implementation units (U0-U11) shipped on `feat/sprint-8-real-auth` cut from `v0.10.1-sprint-7.5`. **Doc-review applied 18 fixes before execution** (1 P0 + 9 P1 + 8 P2). 118 Auth.UnitTests + 44 Migrate.UnitTests + 370 frontend Vitest tests passing. **Sprint-9+ deferrals**: MFA/TOTP, OAuth/social login, account lockout, password-reset email, httpOnly-cookie session, chain-aware reuse-detection across the grace boundary. See [Sprint-8 sign-off](docs/phase-gates/2026-05-20-sprint-8-signoff.md). **Previous**: [Sprint-7.5 sign-off](docs/phase-gates/2026-05-20-sprint-7.5-signoff.md) (`v0.10.1-sprint-7.5`). **Next**: Sprint-8.5 (test sweep + Sprint-7.5 build-error follow-up + OwnerSeed integration test), Sprint-9 (RBAC + MFA hardening), Phase-3 polish (observability dashboards + rate limiting + audit log table).

---

**Sprint-7.5 Production-Ready Trade-off Closures complete (2026-05-20)** — tagged `v0.10.1-sprint-7.5`. Closes the seven Sprint-6 / Sprint-7 deferred trade-offs plus a cross-table index audit over 12 implementation units. Posture flipped from portfolio-demo-grade to **production-ready, big-data-seed-ready**. U1+U2 paired commits flip the wire format to camelCase across all 7 module APIs + SignalR hub protocol + 33 frontend files. U3 retires the `InMemorySkuMetadataStore` singleton in favor of a real per-tenant `skus` table with 10 rich columns (name/category/threshold/weight_grams/dimensions jsonb/description/image_url/barcode/brand/is_flash_sale) + 3 production indexes (btree category, partial btree is_flash_sale, partial UNIQUE barcode). U4 ships the Owner-facing `EditSkuModal` 10-field form + `PUT /api/v1/inventory/skus/{sku}` endpoint. U5 wires the flash-sale outbox dual-write via `SkuFlashSaleChangedV1` cross-module event with an OccurredAt guard in `SkuFlagRepository.UpsertAsync` that rejects stale writes — handles flap A→B→A convergence under W6 competing consumers without relying on per-(tenant, sku) FIFO ordering. U6 ships opaque base64-JSON cursor pagination on the reservation ledger + a new composite btree `(sku, created_at DESC, id DESC)` index + `<LedgerDrawer>` Load more / "All entries loaded" UX. U7 establishes the `useFilterSearchParams<TSchema>` generic helper on TanStack Router primitives — full URL state for filter+sort+page+drawer + filter/sort-change-resets-page convention; D-005 stale-deep-link recovery + D-006 reset rule baked into the hook so every future module list page inherits both. U8 ships a composite UNIQUE on `outbound_saga_transitions (order_id, occurred_at, to_state)` + a `SagaTransitionDuplicateInterceptor` — **plan KTD6 design correction landed during implementation**: EF Core's `SaveChangesFailedAsync` can't actually suppress exceptions, so the primary mechanism shifted to `SavingChangesAsync` pre-check + detach (the 23505 never fires in the happy redelivery path because the duplicate is detached before INSERT). U9 expands SagaPipeline 8 → 9 nodes (Created + AwaitingReservation un-collapsed on the order detail route; OrdersTable cell-level indicator deliberately unchanged per D-007). U10 ships 5 per-module index audit migrations using `CREATE INDEX IF NOT EXISTS` for big-data hot-path queries (composite filter+sort + FK-side + time-range indexes). **Real auth (Sprint-6 trade-off #8)** + **CREATE INDEX CONCURRENTLY** + **million-row latency benchmark** + **big-data seed loader** carry forward. **Subagent dispatch mode wall**: U2/U7/U8/U9 shipped via background subagent dispatch; mid-sprint UX feedback ("dispatch too slow for me") pivoted U3/U6/U4/U5/U10/U11 to inline execution. Both modes produced commit-quality work; inline is faster when the orchestrator has rich context already in-window. See [Sprint-7.5 sign-off](docs/phase-gates/2026-05-20-sprint-7.5-signoff.md). **Previous**: [Sprint-7 sign-off](docs/phase-gates/2026-05-19-sprint-7-signoff.md) (`v0.10.0-sprint-7-orders`). **Next**: Sprint-8 (real auth + first multi-role surface), Sprint-5.5 (parallel-track scale-gate harness), Phase-3 polish (observability dashboards + Gateway hardening + portfolio README + demo video), big-data seed loader (the workstream the Sprint-7.5 production-readiness posture was sized for), or new module frontend surface (Inbound POs UI / Channel mappings UI / Analytics dashboards — every future slice now inherits camelCase wire + URL-state + cursor pagination + 10-col SKU model from day one).

**Sprint-7 Orders Saga Visualisation complete (2026-05-19)** — tagged `v0.10.0-sprint-7-orders`. Second frontend vertical slice ships at `/orders` + `/orders/$orderId`: horizontal `<SagaPipeline>` watching the 11-state fulfillment saga (Placed → Reserved → AwaitingPick → … → Shipped) with sub-second freshness via SignalR push; `<TransitionsLog>` newest-first feed with `aria-live="polite"`; `<OrderLineItems>` (KTD11 cell-level button) reuses Sprint-6's `<LedgerDrawer>` via a `SkuListItem` stub adapter. **Closes Sprint-6 trade-off #9** by lifting SignalR into shared infrastructure under a **single hub-host topology** (only `Outbound.Api` maps `/hub` + registers the two relay consumers `StockChangedRelayConsumer` + `SagaTransitionedRelayConsumer`; Gateway routes `/hub` there permanently — avoids the RabbitMQ competing-consumer trap on the eventual W6 split). New `outbound_saga_transitions` per-tenant audit table written by `SagaTransitionObserver` via explicit per-branch `.ThenAsync` hooks at every `TransitionTo` site in `FulfillmentSaga` (9 transitions instrumented incl. `WhenEnter` IfElse Path A + `If` counter-drain + compound `Reserved → AwaitingPick` chain). JwtBearer lifted into `AddShopFlowDefaults` (closes Sprint-6 trade-off #8 in spirit; real auth still Sprint-8) + access-token query-parameter redaction in `JwtBearerEvents.OnMessageReceived` so `?access_token=` never leaks to request logs. Dev-mode `POST /api/outbound/orders/seed` returns 404 + `environment_not_dev` outside Development. Frontend: ~351 Vitest tests (+130 from Sprint-6); 10 axe-smoke assertions; `@microsoft/signalr ^8.0.7`; `useInventoryQuery` + `useOrdersQuery` toggle `refetchInterval` based on hub state (R13 polling fallback intact). **Doc-review pipeline executed** before start: 5 P1 findings + 2 architectural premise challenges walked through; 1 `safe_auto` fix applied (Auth.Api dropped from hub mapping); 3 P1s declared resolved + 2 routed via user-decision (single-hub-host + IStateObserver-via-explicit-wiring) before U1. **Subagent dispatch mode shipped**: 14 units across 5 rounds (2 inline + 3 parallel — 5+3+2 subagents); orchestrator reviewed each diff + committed serially. See [Sprint-7 sign-off](docs/phase-gates/2026-05-19-sprint-7-signoff.md). **Previous**: [Sprint-6 sign-off](docs/phase-gates/2026-05-19-sprint-6-signoff.md) (`v0.9.0-frontend-vertical-slice`). **Next**: Sprint-7.5 (bundled Sprint-6 trade-off closures — cosmetic SKU schema + camelCase wire + flash-sale dual-write + URL-search-params), Sprint-8 (real auth + first multi-role surface), Sprint-5.5 (parallel-track scale-gate harness), or Phase-3 polish.

**Sprint-6 Frontend Vertical Slice complete (2026-05-19)** — tagged `v0.9.0-frontend-vertical-slice`. First frontend surface ships in a new top-level `web/` subdirectory: Vite 5 + React 19 + TypeScript strict + TanStack Router (file-based) + TanStack Query (2-s polling) + Zustand. Inventory screen × Owner role end-to-end through real `Inventory.Api` WRITE controllers (`POST /adjustments`, `PUT /skus/{sku}/threshold`, `PUT /skus/{sku}/flash-sale`, `POST /skus`), with `Adjust Stock` modal + inline threshold edit (optimistic UI via React 19 set-state-during-render) + `Flash-sale` toggle + `Create SKU` modal. 8 other screens ship as `ComingSoon` placeholders behind the auth guard. Stub `Auth.Api` returns a baked JWT (Sprint-7 → real auth). Frontend CI job runs typecheck + lint + 221 Vitest tests (incl. 6 axe-clean a11y smoke assertions) + build in parallel with .NET jobs. **KTD11** (emergent in U13): `nested-interactive` axe violation caught by the new harness — SkuTable refactored so the row drops button semantics + first-column SKU cell hosts the button. See [Sprint-6 sign-off](docs/phase-gates/2026-05-19-sprint-6-signoff.md). **Previous**: [Methodology Writeup sign-off](docs/phase-gates/2026-05-18-methodology-writeup-signoff.md) (`v0.8.0-methodology-writeup`).

**Phase-3 Methodology Writeup complete (2026-05-18)** — tagged `v0.8.0-methodology-writeup`. Ships [docs/methodology.md](docs/methodology.md) (~8500 words) — a comprehensive 7-sprint chronological case study + synthesis pattern catalog + friction section documenting the AI-assisted development methodology used to build ShopFlow WMS. Audience: future-self + developers cloning the repo (not HR/recruiter). Output: 100% in `docs/`; no code changes. Sign-off: [docs/phase-gates/2026-05-18-methodology-writeup-signoff.md](docs/phase-gates/2026-05-18-methodology-writeup-signoff.md). **Previous**: [Sprint-5 sign-off](docs/phase-gates/2026-05-17-sprint-5-signoff.md) (`v0.7.0-sprint-5`). **Next**: Sprint-5.5 (close scale-gate harness — see methodology friction mode 4), Sprint-6 (Analytics — W9-W10), or public blog derivative.

**Phase-2 Sprint-5 complete (2026-05-17)** — tagged `v0.7.0-sprint-5`. Closes Phase-2 egress with a new `ShopFlow.StockSync` module (7th logical module in the modular monolith) consuming Inventory's stock-mutation outbox stream and pushing per-channel `available_to_sell` updates through a four-layer isolation pipeline: coalescing buffer per `(tenant, sku, channel)` → per-tenant priority queue (high/normal lanes for flash-sale routing) → token bucket per `(tenant, channel)` → Polly v8 circuit breaker → `IChannelAdapter.PushStockUpdateAsync` → push-log audit row. **KTD1** replaces the literal brainstorm R1 (consume 3 transition events) with a single canonical `StockLevelChangedV1` event emitted from Inventory's 5 stock-mutating repository paths + the put-away `AdjustAtBin` path — clean cross-module shape, no Outbound coupling. `ShopeeAdapter.PushStockUpdateAsync` body fills the Sprint-4 stub with real HTTP POST + status-code → stable error-code mapping; Shopee mock gains `/api/v2/product/update_stock` + chaos toggle. SkuFlag admin API + caching wrapper (5min TTL, opens DI scope + binds tenant via K12 pattern from singleton context). 359 unit tests green (+71 from Sprint-5). +24 `Category=Integration` tests. +2 `Category=Load` scale-gate slots Skip'd per Sprint-4 U9 precedent — production primitives all proven by U3-U8 unit + integration coverage; wall-time noisy-neighbor measurement deferred to Sprint-5.5 follow-up (multi-tenant Aspire boot + real Shopee mock alongside StockSync.Api). See [Sprint-5 sign-off](docs/phase-gates/2026-05-17-sprint-5-signoff.md). **Previous**: [Sprint-4.5 sign-off](docs/phase-gates/2026-05-15-sprint-4.5-signoff.md) (`v0.6.1-sprint-4.5`). **Next**: Sprint-5.5 (close scale-gate harness), Sprint-6 (Analytics module W9-W10 — read-side projections), or Phase-3 polish (Gateway hardening, observability, portfolio README + demo).

**Phase-2 Sprint-4.5 complete (2026-05-15)** — tagged `v0.6.1-sprint-4.5`. Closes the four Sprint-4 sign-off deferrals as a ~1-week point release: receiver `provider_event_id` now sourced from the marketplace-asserted Shopee `event_id` via `IChannelAdapterFactory.ResolveFor(channelType).ParseWebhook(...)` (body-hash stub deleted); `WebhookOrchestrator` gates on `event_type == "order.created"`, resolves per-line external→internal SKUs via `IProductMappingService`, and emits the canonical `OrderImportedV1` (or **fails the whole import** per contract canon when any line is unmapped — brainstorm R6 reversal documented); `TenantWebhookHarness` integration helper with `WebApplicationFactory<Program>`-backed Channel.Api host + multi-tenant Postgres provisioning; three `Category=Load` scale-gate bodies in place (burst-200rps × 5 tenants + replay-100× idempotency + cross-tenant signature → 401). 288 unit tests green (+19 from Sprint-4.5: 13 ShopeeAdapterParseOrderCreated + 7 WebhookOrchestrator). 1 new `Category=Integration` smoke test; 3 `Category=Load` tests runnable in CI. See [Sprint-4.5 sign-off](docs/phase-gates/2026-05-15-sprint-4.5-signoff.md). **Previous**: [Sprint-4 sign-off](docs/phase-gates/2026-05-13-sprint-4-signoff.md) (`v0.6.0-sprint-4`). **Next**: Sprint-5 Stock Sync Engine (Phase-2 W6-W8 centerpiece — coalescing buffer + per-channel token bucket + priority queue + circuit breaker + allocation engine; noisy-neighbor scale gate).

**Phase-1 Sprint-3-redux complete (2026-05-13)** — tagged `v0.5.0-sprint-3-redux`. **Phase-1 customer funnel closed**: Inventory holds stock (Sprint-1-redux), Inbound fills it (Sprint-2-redux), Outbound drains it (Sprint-3-redux). The Outbound module ships the full fulfillment saga (MassTransit state machine, 11 states, EF saga repository with K12 per-tenant DbContext binding via `TenantBindingSagaFilter`), 9 cross-module contracts, 3 new Inventory consumers wrapping the extended `ReservationRepository` (`TryReserveLinesAsync` + `ReleaseLinesAsync` — atomic multi-line CTE; `reservations_ledger` schema gained `order_line_id` with composite UNIQUE), `IPickQueue` per-tenant `Channel<PickRequestV1>` + `PickWaveGeneratorService` (15-min window batching, round-robin picker), mocked shipping carrier with Polly v8 retry, pick-failure compensation via Set-based release dedup, and `OrderCancelledConsumer` propagating saga terminal state to the Order row. K15 verified: `MassTransit.EntityFrameworkCore` 8.3.4 + EF Core 9 bind cleanly. K11 multi-row CTE concurrency fix landed as institutional learning ([docs/solutions/2026-05-13-multi-row-cte-predicate-must-live-in-update.md](docs/solutions/2026-05-13-multi-row-cte-predicate-must-live-in-update.md)) — pre-check CTEs are unsafe under READ COMMITTED; predicate must live inside the UPDATE. W5 scale gate (`Category=Load`): 2000 orders × 3 tenants, dev-laptop Shipped p99 247-332ms/tenant + Cancelled p99 112-131ms/tenant + fairness floor 0.918-0.979 (operator-pipeline path; saga bypassed — documented limitation; production CI re-validates). ~270 unit + ~120 integration + 4 load tests green. See [Sprint-3-redux sign-off](docs/phase-gates/2026-05-13-sprint-3-redux-signoff.md). **Previous**: [Sprint-2.5 sign-off](docs/phase-gates/2026-05-13-sprint-2.5-signoff.md) (`v0.4.1-sprint-2.5`). **Next**: Phase-2 Sprint-4 (Channel Connections + webhook idempotency) cuts from `v0.5.0-sprint-3-redux`.

**Phase-1 Sprint-2-redux complete (2026-05-13)** — tagged `v0.4.0-sprint-2-redux`. The Inbound module ships: PurchaseOrder + Receiving aggregates with auto-state-transition state machine, per-line confirmation via `ConfirmReceivingLineService` (writes the ledger + outbox row atomically), append-only `reconciliation_tickets` log on quantity mismatch, thin HTTP controllers. Inventory schema gains zones / bins / stock_item_bins / inbound_dedup tables + nullable `home_zone_id` FK on stock_items. New cross-module flow: `ShopFlow.Contracts.Inbound.InboundConfirmedV1` event flows through the outbox dispatcher to `InboundConfirmedConsumer` in Inventory, which auto-creates stock_items, applies bin-targeted stock change via the new `AdjustAtBinAsync` (UPSERT stock_items + UPSERT stock_item_bins + UPDATE bins.occupancy_qty + INSERT stock_adjustments — all in one ReadCommitted transaction), and dedups against the `inbound_dedup(receiving_id, line_id)` table. MassTransit transport flipped from in-memory to real RabbitMQ via a config knob (`ShopFlowDefaultsOptions.MessageBusTransport`) — promoted from W6 to W4 so Sprint-3-redux's saga inherits production-shape broker semantics. 110 unit tests + 52 integration tests green. Architecture finding [docs/solutions/2026-05-13-cross-module-outbox-table-name-collision.md](docs/solutions/2026-05-13-cross-module-outbox-table-name-collision.md) captures U9's deferred cross-module flow test (Sprint-2.5 candidate). See [Sprint-2-redux sign-off](docs/phase-gates/2026-05-13-sprint-2-redux-signoff.md) for measured numbers + deviations. **Previous**: [Sprint-1-redux sign-off](docs/phase-gates/2026-05-12-sprint-1-redux-signoff.md) (`v0.3.0-sprint-1-redux`). **Next**: Sprint-2.5 (outbox table-name rename) or Sprint-3-redux (Outbound + saga) — plans TBD.

Multi-tenancy redesign captured under [ADR-0003](docs/adr/0003-database-per-tenant-for-compliance.md): **database-per-tenant on shared Postgres cluster** for PDPA SEA hard isolation. Canon: [product plan v3.0](docs/redesign/01-product-development-plan.md), [tech design v3.0](docs/redesign/02-technical-design-document.md). The historical Phase-0 + Sprint-1 work is preserved at branch `archive/phase-1-sprint-1-rls-shared` and tag `archive/v0.1.0-phase-0-rls-shared`.

See [docs/CHANGELOG.md](docs/CHANGELOG.md) for the supersession record.

## What this is

A warehouse management system designed for SME registered businesses running 1-5K SKUs across 2-5 marketplaces with 100-1K orders/day. The thesis is **bounded sync latency with correctness guarantees at flash-sale load, delivered with database-per-tenant hard isolation that reads cleanly under PDPA audit**. Built at MVP scope (5 production-ready tenants, single Postgres cluster, mocked channel APIs) but designed so the path to **25-50 validated tenants under noisy-neighbor load** is concrete.

The full thesis with scale targets, SLOs, ADRs, and tier-by-tier rollout lives in two source-of-truth documents at the repo root: [`01-product-development-plan.md.docx`](01-product-development-plan.md.docx) (product) and [`02-technical-design-document.md.docx`](02-technical-design-document.md.docx) (architecture). The redesigned v3.0 markdown drafts are in [`docs/redesign/`](docs/redesign/); the .docx files are scheduled to be regenerated from those drafts.

## Architecture stance

Six bounded contexts (Inventory, Inbound, Outbound, Channel, Analytics, Gateway), bootstrapped as a **modular monolith** ([ADR-0002](docs/adr/0002-modular-monolith-first.md)) — one .NET solution, six logical modules in separate `.csproj` per bounded context, single host, in-memory MediatR. Mechanical 6-service split is a planned **W6 event** triggered by the channel adapter framework's arrival.

Multi-tenancy is **database-per-tenant on a shared Postgres cluster** ([ADR-0003](docs/adr/0003-database-per-tenant-for-compliance.md)). Each tenant maps to one logical Postgres DATABASE; routing happens in middleware (header → JWT claim → subdomain priority); PgBouncer in transaction-pooling mode is the connection multiplexer. A separate `shopflow_control` database holds the tenant catalog. Right-to-erasure is `DROP DATABASE` after retention window.

**Stack**: C# .NET 9 (canon declares .NET 8; Phase-0-redux pins net9.0 via `global.json` because that is the SDK available on the developer machine — see [Directory.Build.props](Directory.Build.props)), Postgres 16, **PgBouncer**, Redis, RabbitMQ, MassTransit (sagas + outbox), OpenTelemetry, Aspire AppHost (dev) + hand-maintained Docker Compose (production handoff per [ADR-0001](docs/adr/0001-aspire-vs-docker-compose.md)).

## Compliance posture

PDPA Vietnam (Decree 13/2023/ND-CP) + Singapore PDPA. Hard isolation answers "how do you guarantee data segregation?" with two databases, two backups, two `DROP DATABASE` blast radii. SOC2 / ISO 27001 are explicit non-goals at this stage; the architecture supports them, the controls work is operational follow-up. See [product plan v3.0 §4](docs/redesign/01-product-development-plan.md#4-compliance-scope).

## Repo layout

```
.
├── AGENTS.md                      AI-pair-programming rule canon (auto-loaded by Claude/Cursor/Copilot)
├── CLAUDE.md                      project context for AI assistants
├── README.md                      this file
├── 01-product-development-plan.md.docx    canonical product spec (v3.0 draft in docs/redesign/)
├── 02-technical-design-document.md.docx   canonical tech design (v3.0 draft in docs/redesign/)
├── docs/
│   ├── adr/                       numbered architectural decisions (immutable; postscripts allowed)
│   ├── plans/                     active and superseded work plans
│   ├── redesign/                  v3.0 markdown drafts (to be regenerated to .docx)
│   ├── solutions/                 compounding learnings
│   ├── source/                    .docx → .txt extracts (gitignored)
│   ├── ideation/                  ranked candidate ideas
│   └── CHANGELOG.md               canon supersession history
├── src/
│   ├── ApiGateway/ShopFlow.Gateway/        YARP reverse proxy
│   ├── AppHost/ShopFlow.AppHost/           Aspire dev orchestrator
│   ├── ControlPlane/                       tenant catalog + provisioning catalog DB
│   ├── Services/{Inventory,Inbound,Outbound,Channel,Analytics}/   bounded contexts
│   └── Shared/{SharedKernel,SharedKernel.Analyzers,Contracts}/    cross-cutting + Roslyn rules
├── tests/                                  unit + integration test projects
├── infrastructure/                         pgbouncer + docker-compose production handoff
└── tools/
    ├── extract-docs.{sh,ps1}               .docx text extraction
    ├── shopflow-migrate/                   per-tenant migration runner CLI
    └── shopflow-gate/                      phase-gate runner CLI
```

## Getting started

```
task setup        # install dotnet tools (CSharpier, Husky.NET) + pre-commit hook
dotnet build      # 0 warnings / 0 errors expected
dotnet test --filter "Category!=Integration&Category!=Load"   # 80 unit tests pass
task up           # Aspire dev orchestrator — needs Docker
```

Reading order for new contributors:

1. [Sprint-1-redux reservation ledger plan](docs/plans/2026-05-11-003-phase-1-sprint-1-redux-reservation-ledger-plan.md) — next implementation slice.
2. [Phase-0-redux sign-off](docs/phase-gates/2026-05-12-phase-0-redux-signoff.md) — what landed, what's deferred.
3. [Tech design v3.0](docs/redesign/02-technical-design-document.md) §1 (multi-tenancy), §2 (provisioning), §4 (reservation ledger).
4. [ADR-0003](docs/adr/0003-database-per-tenant-for-compliance.md) — the supersession decision.
5. [AGENTS.md](AGENTS.md) — the executable rule canon.

## Historical reference

Phase-0 (the v2.0 RLS-shared design) and Phase-1 Sprint-1 work-in-progress live at:
- Branch: `archive/phase-1-sprint-1-rls-shared`
- Tag: `archive/v0.1.0-phase-0-rls-shared` (annotated supersession note)

Both remain in the remote forever as the "what we redesigned away from" reference. Three `docs/solutions/` learnings carry forward: EF migration attributes, FsCheck Replay format, green-against-stub property pattern.

## License

TBD.
