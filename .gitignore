# Derived outputs from tools/extract-docs.{sh,ps1} — re-runnable, not source of truth.
docs/source/

# Claude Code per-user settings (commit .claude/settings.json if you want shared config).
.claude/settings.local.json

# Claude Code subagent worktrees (per-agent isolated working trees).
.claude/worktrees/

# OS
Thumbs.db
.DS_Store

# IDE / editors
.vs/
.vscode/
.idea/
*.user

# .NET build outputs
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
TestResults/
.dotnet/
*.user
*.suo
*.log
*.tmp
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json

# MSBuild diagnostics
*.binlog
msbuild.log

# Husky.NET — _/ contents are auto-generated by `dotnet husky install`.
# Commit .husky/pre-commit, gitignore everything under _/.
.husky/_/

# Task (Taskfile.dev) checksum cache — used by `sources:` declarations.
.task/

# Coverage outputs
coverage/
*.coverage
*.coveragexml

# Aspire AppHost
.aspire/

# Node (root-level tool installs; the web/ frontend has its own ignores)
node_modules/
**/node_modules/
