/**
 * Sprint-6 U1 placeholder shell.
 *
 * Real router + auth + Inventory screen land in U6 + U9.
 * This component exists so the Vite scaffold has a renderable App
 * for `pnpm dev` + `pnpm build` smoke testing.
 */
export default function App() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1 style={{ margin: 0 }}>ShopFlow WMS</h1>
      <p style={{ color: '#666', marginTop: '0.5rem' }}>
        Sprint-6 scaffold · vertical slice landing in U6.
      </p>
    </main>
  );
}
