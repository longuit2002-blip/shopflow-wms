namespace ShopFlow.Auth.Domain;

/// <summary>
/// Placeholder so the Auth.Domain csproj has at least one type and the
/// module shape is locked into CI (Sprint-6 plan U4). Sprint-7 introduces
/// the real aggregates (User, Session, RefreshToken, RoleAssignment).
/// Delete this marker when the first real type lands; it has no behavior.
/// </summary>
public static class AuthModuleMarker
{
    public const string ModuleName = "Auth";
}
