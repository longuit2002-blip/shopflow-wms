namespace ShopFlow.Outbound.Domain;

/// <summary>
/// Placeholder so the Outbound.Domain csproj has at least one type and the
/// module shape is locked into CI (plan U9). Phase-1 Sprint-3 introduces
/// the actual aggregates (FulfillmentOrder, ShipmentLeg). Delete this
/// marker when the first real type lands; it has no behavior.
/// </summary>
public static class OutboundModuleMarker
{
    public const string ModuleName = "Outbound";
}
