namespace ShopFlow.Outbound.Application;

/// <summary>
/// Placeholder so the Outbound.Application csproj has at least one type and
/// the module shape is locked into CI (plan U9). Real MediatR commands +
/// handlers land in Phase-1+. Delete this marker when the first real
/// handler is committed.
/// </summary>
public static class OutboundApplicationMarker
{
    public const string ModuleName = "Outbound";
}
