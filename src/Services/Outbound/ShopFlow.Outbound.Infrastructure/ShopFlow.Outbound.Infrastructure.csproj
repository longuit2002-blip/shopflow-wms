<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Outbound module — Infrastructure. Sprint-3-redux U1 ships the
    OutboundDbContext + initial schema (orders / order_lines / pick_waves /
    pick_assignments / pickers / saga_state / outbound_outbox_messages) +
    AddOutboundModule composition root. MassTransit.EntityFrameworkCore is
    a prerequisite for U4's saga repository (K15 smoke build verifies the
    MT 8.3.4 + EF Core 9 binding here before U4 starts).
  -->

  <ItemGroup>
    <PackageReference Include="MassTransit.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Relational" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ShopFlow.Outbound.Application\ShopFlow.Outbound.Application.csproj" />
    <ProjectReference Include="..\ShopFlow.Outbound.Domain\ShopFlow.Outbound.Domain.csproj" />
    <ProjectReference Include="..\..\..\Shared\ShopFlow.SharedKernel\ShopFlow.SharedKernel.csproj" />
  </ItemGroup>

</Project>
