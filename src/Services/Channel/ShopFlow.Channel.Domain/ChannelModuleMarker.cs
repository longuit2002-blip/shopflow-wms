namespace ShopFlow.Channel.Domain;

/// <summary>
/// Placeholder so the Channel.Domain csproj has at least one type and the
/// module shape is locked into CI (plan U9). Phase-1 Sprint-2 introduces
/// the actual aggregates (ChannelConnection, ChannelSyncToken). Delete this
/// marker when the first real type lands; it has no behavior.
/// </summary>
public static class ChannelModuleMarker
{
    public const string ModuleName = "Channel";
}
