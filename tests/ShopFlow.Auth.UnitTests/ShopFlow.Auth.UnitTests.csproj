<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Sprint-6 plan U4 — Auth.Api dev-mode fake login smoke tests. Two cases:
      • Happy POST /auth/login → 200 + JWT with tenant_slug + role + exp claims.
      • Empty body → 400 ProblemDetails.
    Uses WebApplicationFactory<Program> for in-process Kestrel boot.
  -->

  <PropertyGroup>
    <RootNamespace>ShopFlow.Auth.UnitTests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
    <PackageReference Include="Microsoft.IdentityModel.JsonWebTokens" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Services\Auth\ShopFlow.Auth.Api\ShopFlow.Auth.Api.csproj" />
  </ItemGroup>

</Project>
