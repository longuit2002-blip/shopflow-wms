# ShopFlow WMS — Project Context for AI Assistants

This file is auto-loaded by Claude Code (and respected as a fallback by other agents). It captures the project context that should ship with the source. The user works across multiple computers — anything project-related belongs here in the source tree, not in machine-local memory.

## What this project is

**ShopFlow WMS** — 12-week single-developer portfolio Warehouse Management System for SEA marketplaces (Shopee, Lazada, TikTok Shop, Shopify). Source is being bootstrapped from scratch as of late April 2026.

**Stack**: C# .NET 8, Next.js 14 App Router + React Query + SignalR, Postgres 16, **PgBouncer** (transaction-pooling), Redis, RabbitMQ, MassTransit (sagas + outbox), OpenTelemetry, Aspire AppHost (dev) + hand-maintained Docker Compose (prod). Six modular-monolith microservices internally: Gateway (YARP), Inventory, Inbound, Outbound, Channel, Analytics.

**Engineering anchors**:
- **Database-per-tenant** on shared Postgres cluster ([ADR-0003](./docs/adr/0003-database-per-tenant-for-compliance.md)) — PDPA SEA hard isolation. Routing per-request via middleware; control-plane catalog DB; right-to-erasure is `DROP DATABASE`.
- Append-only **reservation ledger** (CTE-based conditional INSERT at READ COMMITTED, not row lock) — the hot-key flash-sale solution.
- **Stock sync engine** with coalescing buffer + per-channel token bucket + priority queue, scoped per-tenant.
- Persistent **webhook idempotency** via Postgres `UNIQUE(channel_id, provider_event_id)` per tenant DB.
- **Outbox pattern** per-tenant with multiplexed dispatcher; dispatcher path: polling → LISTEN/NOTIFY → Debezium CDC at scale.
- **MassTransit saga** for fulfillment orchestration (Reserve → Pick → Pack → Ship with compensation), tenant context per-message via headers.

## Source documents (canonical)

- [01-product-development-plan.md.docx](./01-product-development-plan.md.docx) — v2.0 (April 2026); v3.0 markdown draft at [docs/redesign/01-product-development-plan.md](./docs/redesign/01-product-development-plan.md). The .docx will be regenerated from the v3.0 draft.
- [02-technical-design-document.md.docx](./02-technical-design-document.md.docx) — v2.0; v3.0 markdown draft at [docs/redesign/02-technical-design-document.md](./docs/redesign/02-technical-design-document.md). Same regeneration plan.

These are .docx (Word) files. To extract text for grep/search, run [tools/extract-docs.sh](./tools/extract-docs.sh) (bash) or [tools/extract-docs.ps1](./tools/extract-docs.ps1) (PowerShell). The script writes plain-text equivalents to `docs/source/` (gitignored) — re-runnable on any machine.

**v3.0 changes summary**: multi-tenancy is now §1 (was §4). Tenancy model is database-per-tenant on shared Postgres cluster ([ADR-0003](./docs/adr/0003-database-per-tenant-for-compliance.md)). The reservation ledger conditional CTE INSERT runs at READ COMMITTED (was SERIALIZABLE in v2.0 — corrected per Postgres docs). Outbox is per-tenant with multiplexed dispatcher.

## Bootstrap stance (decided 2026-04-27)

Per [docs/ideation/2026-04-27-shopflow-wms-bootstrap-ideation.md](./docs/ideation/2026-04-27-shopflow-wms-bootstrap-ideation.md), Phase 0-1 ships **ONE container as a modular monolith** with six logical modules in separate `.csproj` per bounded context. Mechanical split into 6 microservice processes is a planned **W6 event** when the channel adapter framework arrives and async cross-process messaging actually pays its freight. README opens with the eventual 6-service diagram and labels Phase 0-1 as "modular monolith stage."

Top-7 bootstrap ideas captured in the ideation doc above. Recommended W0 / W1 / W2 sequence is in that file's "Recommended Bootstrap Sequence" section.

## Hard non-negotiables (from the design)

- **Correctness over latency.** Oversell is a correctness bug, not a performance bug. Reject ambiguous orders rather than queuing optimistically.
- **Idempotency everywhere.** Every consumer, webhook receiver, external-API call must be idempotent.
- **Multi-tenancy from day 1.** `tenant_id` on every row + RLS policies even at MVP single-tenant. The cheapest scale decision in the whole design (Tech Design §4.5).
- **Observability built in Phase 0**, not retrofitted. Correlation ID + W3C TraceContext propagated through every service.
- **No cloud lock-in.** Docker Compose for dev; production path is plain containers on any orchestrator.

## Working preferences

- **Cross-machine workflow.** User zips and ships the source between computers. Anything project-related — context, scripts, decisions, ideation, ADRs — must live inside this directory tree, not in `~/.claude/projects/...` or other machine-local locations.
- **Source docs are .docx**, not markdown (with v3.0 markdown drafts in `docs/redesign/` pending .docx regeneration). Read via the extraction scripts under `tools/`. Treat the .docx as the source of truth; do not edit the extracted .txt as if they were originals.
- **Compounding learnings**: when a fix is non-obvious, capture in [`docs/solutions/`](./docs/solutions/). The "every reviewer comment is a missing rule" pattern is canon (AGENTS.md).

## Current stage

**Multi-tenancy redesign accepted (2026-05-11)**. Phase-0 (RLS-shared) and Phase-1 Sprint-1 work-in-progress are archived at `archive/phase-1-sprint-1-rls-shared` branch and `archive/v0.1.0-phase-0-rls-shared` tag. The system pivots to **database-per-tenant on shared Postgres cluster** under [ADR-0003](./docs/adr/0003-database-per-tenant-for-compliance.md).

**Active branch**: `feat/sprint-8-real-auth` (cut from `v0.10.1-sprint-7.5`).

**Sprint-8 Real Auth Module is complete.** Tag: `v0.11.0-sprint-8`. Sign-off: [`docs/phase-gates/2026-05-20-sprint-8-signoff.md`](./docs/phase-gates/2026-05-20-sprint-8-signoff.md). Plan: [`docs/plans/2026-05-20-001-feat-sprint-8-real-auth-plan.md`](./docs/plans/2026-05-20-001-feat-sprint-8-real-auth-plan.md). Brainstorm: [`docs/brainstorms/2026-05-20-sprint-8-real-auth-requirements.md`](./docs/brainstorms/2026-05-20-sprint-8-real-auth-requirements.md). Retires the Sprint-6 dev-mode baked JWT in favor of: Argon2id (Konscious) password hashing with OWASP 2026 PHC parameters baked in; HS256 JWT access tokens (15-min TTL) issued by the Auth module + validated by the kernel JwtBearer (shared `Auth:DevSecret` — KTD5); opaque 32-byte refresh tokens in Redis with 60-sec grace-window tombstone rotation (OWASP refined pattern, ADV-002 mitigation); per-tenant `users` table with `ux_users_email_lower` UNIQUE expression-index + `chk_users_role` CHECK constraint + partial `ix_users_role_active`; R6 enumeration prevention (all credential failures collapse to `auth.invalid_credentials` + 401); Owner-only admin surface with consolidated discriminated `UpdateUserCommand` (KTD8); subdomain-first tenant resolution with host-suffix allowlist (SEC-004) + `ReservedSlugs` denylist (ADV-001); frontend token-pair store + transparent 401 → refresh interceptor + rememberMe + subdomain-detect + Sidebar Logout; shopflow-migrate `provision` owner-seed extension + new `seed-owner` subcommand for legacy-tenant retrofit (ADV-003). **Doc-review applied 18 fixes before execution** (1 P0 + 9 P1 + 8 P2). **12 implementation units** (U0-U11) across 12 commits.

**Sprint-8 units**:
- U0 — Branch cut + brainstorm + plan + 10 post-doc-review KTDs in opening commit body (`b5b7eec`)
- U1 — Auth.Domain `User` aggregate + `UserRole` enum (pinned to DB CHECK via `UserRoleTests`) + 3 domain events; 38 unit tests (`86a1d9a`)
- U2 — Auth.Application ports + DTOs (`IUserRepository` / `IPasswordHasher` / `IRefreshTokenStore` with `RefreshRotateOutcome` enum / `ITokenIssuer`; wire DTOs for login/refresh/logout/changePwd + admin surface); pure interfaces (`40b377a`)
- U3 — Auth.Infrastructure: `AuthDbContext` + `UserConfiguration` (role-as-string conversion) + `AddUsers` migration (`ux_users_email_lower` + `chk_users_role` + partial `ix_users_role_active`) + `UserRepository` with 23505 → EmailInUse Result wrap + `AuthServiceCollectionExtensions` composition + IntegrationTests skeleton (6 repo tests + 5 migration smoke tests) (`1d9d13f`)
- U4 — `Argon2idPasswordHasher` (Konscious 1.3.1) with PHC-embedded parameters (OWASP 2026 defaults); never-throw `Verify` for malformed hashes; 18 unit tests (`e947d80`)
- U5 — `RedisRefreshTokenStore` — atomic Lua-scripted rotation + 60-sec grace-window tombstone; SHA-256 hash in key name, per-tenant namespacing; rememberMe carries through rotation TTL bucket; 9 Testcontainers Redis integration tests (`a616193`)
- U6 — `JwtTokenIssuer` reads iss/aud/DevSecret from same Auth config section as the kernel validator (KTD5 — single source of truth); claims sub/email/role/tenant_slug/iss/aud/iat/exp; 11 unit tests incl. round-trip through kernel-shape `TokenValidationParameters` (`3e5f9c7`)
- U7 — Login + Refresh + Logout + ChangePassword MediatR handlers; canonical `auth.invalid_credentials` for every failure leg (R6); maps the 4 RotateAsync outcomes (Issued / GraceReplay / ReuseDetected → `auth.refresh_reused` + RevokeAll cascade / NotFound); ChangePassword post-rotation revoke-all-sessions; 25 unit tests (`ca38151`)
- U8 — Admin handlers: `CreateUserCommand` + consolidated `UpdateUserCommand` (KTD8 — `UpdateUserOperation` discriminator routes SetRole/ResetPassword/Deactivate via switch); `ListUsersQuery` projects to `UserSummary` (no PasswordHash); `PasswordGenerator` (16-char URL-safe, no ambiguous chars, guaranteed 4-category mix); 26 unit tests (`9cf30d0`)
- U9 — Auth.Api real surface: `AuthController` (`[SkipTenantRouting]` + in-controller subdomain resolver with host-suffix allowlist + `ReservedSlugs` denylist + body-fallback + source-conflict 400 + ADV-004 unknown-tenant collapse); `AuthAdminController` (`[Authorize(Roles = "Owner")]`); Program.cs full composition (`AddShopFlowDefaults` + `AddControlPlane` + `AddAuthModule` + `AddShopFlowControllers`); AuthOptions rewrite + appsettings.json rewrite (Argon2 + Refresh sub-sections); Gateway route `/auth/{**catch-all}` → `/api/auth/{**catch-all}` (KTD6); AppHost `WithReference(postgres)+WithReference(redis)` for auth-api; `ReservedSlugs` shared kernel utility (26 entries); Sprint-6 stub controller tests deleted; 3 endpoint-shape tests as CI scaffolding (`720353b`)
- U10 — shopflow-migrate `OwnerSeed` delegates to Auth.Infrastructure (SG-001 — no duplicate hashing); `provision --tenant=<slug>` runs owner-seed after MigrateAsync with `--owner-email` / `--owner-password` / `--owner-password-from-env` flags + one-time stdout temp password echo; new `seed-owner --tenant=<slug>` subcommand for ADV-003 legacy-tenant retrofit; pre-flight `ReservedSlugs` check; Auth module registered in `IModuleMigrationRegistry`; 9 new tests (Migrate.UnitTests total 44) (`9e44661`)
- U11 — Frontend token-pair: `useAuth` rewrite (accessToken + refreshToken + ISO expiries + user.userId; `shopflow.auth.v2` localStorage; back-compat `jwt` + `logout` + `login(jwt)` shim for Sprint-7 useSignalR); `api/auth.ts` rewrite (`/api/auth/login|refresh|logout|me/password` + `LoginRequest` with rememberMe + tenantSlug + `detectTenantFromHost`); httpClient 401 → refresh interceptor with module-scoped `inflightRefresh` guard + idempotency-key persistence across retry; LoginScreen Workspace field (subdomain auto-detect → read-only, fallback editable) + RememberMe checkbox; Sidebar `<UserRow>` with email + role + tenant + LogOut button (best-effort POST logout → clearSession → /login); 7 new useAuth tests; obsolete Sprint-6 LoginScreen + httpClient tests deleted (`47412ec`)

**Sprint-8 trade-offs locked in** (carry into Sprint-9+; full list in sign-off):
1. **MFA / TOTP, OAuth, account lockout, password-reset email** — all R28 explicit Sprint-9+.
2. **httpOnly-cookie session** — Sprint-8 localStorage carries XSS risk; migration is a kernel-level cookie + SignalR auth coordination job.
3. **Chain-aware reuse-detection across grace boundary** — current impl collapses post-grace replay to single-session logout; chain tracking is Sprint-9 hardening.
4. **Frontend `httpClient.test.ts` + `LoginScreen.test.tsx` rewrite** is Sprint-8.5 candidate (Sprint-6 versions deleted; new contracts covered by `useAuth.test.ts` token-pair suite + Auth.IntegrationTests endpoint scaffolding).
5. **Auth integration tests against real Aspire-managed services ship Skip-marked**; CI runs them in Docker containers (Sprint-1+ posture).
6. **OwnerSeed against real Postgres** has unit coverage for flag-resolution + stdout-echo; full integration test is CI-tier follow-up.
7. **Pre-existing Sprint-7.5 build errors** (StockSyncOptions name collision, Channel.UnitTests PredicateBuilder, Outbound.UnitTests Microsoft.Extensions.Time) carry into Sprint-8 branch — not on the Auth+Migrate critical path; Sprint-8.5 sweep candidate.
8. **Subdomain-routed CORS hardening** — Sprint-8 closes Host-header injection via `TrustedHostSuffixes` allowlist; per-origin CORS allowlist + preflight cache is a follow-on.

**Next implementation step**: cut a fresh branch from `v0.11.0-sprint-8` and start one of:
- **Sprint-8.5** — Test sweep (httpClient.test.ts + LoginScreen.test.tsx rewrite) + Sprint-7.5 build-error follow-up (StockSyncOptions / Channel.UnitTests / Outbound.UnitTests) + OwnerSeed real-Postgres integration test. ~1-week point release.
- **Sprint-9** — RBAC + MFA: TOTP placeholder activation, password-reset email, chain-aware reuse-detection, account lockout, per-permission policy gates.
- **Phase-3 polish** — Observability dashboards (auth-failures-per-tenant, refresh-rotations-per-second, grace-replay-rate); rate limiting on /api/auth/login + /api/auth/refresh; `auth_audit_log` table.

---

**Sprint-7.5 Production-Ready Trade-off Closures history** (kept for context; tag `v0.10.1-sprint-7.5`). Sign-off: [`docs/phase-gates/2026-05-20-sprint-7.5-signoff.md`](./docs/phase-gates/2026-05-20-sprint-7.5-signoff.md). Plan: [`docs/plans/2026-05-19-002-feat-sprint-7.5-trade-off-closures-plan.md`](./docs/plans/2026-05-19-002-feat-sprint-7.5-trade-off-closures-plan.md). Brainstorm: [`docs/brainstorms/2026-05-19-sprint-7.5-trade-off-closures-requirements.md`](./docs/brainstorms/2026-05-19-sprint-7.5-trade-off-closures-requirements.md). Closes the seven Sprint-6 / Sprint-7 deferred trade-offs plus a cross-table index audit over **12 implementation units**. Posture flipped from portfolio-demo-grade to **production-ready, big-data-seed-ready**.

**Sprint-7.5 units**:
- U0 — Branch cut + brainstorm + plan + 9 post-doc-review KTDs in opening commit body (`b9fad4e`)
- U1 — `AddShopFlowControllers` helper bundling `AddControllers + AddJsonOptions(CamelCase)` + extends `AddSignalR().AddJsonProtocol(camelCase)` in `AddShopFlowDefaults` + 7 Api Program.cs migrate. 8 unit tests pin round-trip + multi-word + collection + nested + acronym rules (`2ca83d1`)
- U2 — camelCase frontend rename across 33 files (API DTO interfaces + component prop access + Vitest fixtures + a11y smoke + routeTree.gen). 321/324 Vitest pass; 3 pre-existing failures unchanged from Sprint-7 baseline (`15c7297`)
- U3 — `skus` rich catalog table (10 cols: name/category/threshold/weight_grams/dimensions jsonb/description/image_url/barcode/brand/is_flash_sale) + `ISkuRepository` port + EF impl + migration with 3 production indexes (btree category, partial btree is_flash_sale, partial UNIQUE barcode). `InMemorySkuMetadataStore` + `ISkuMetadataStore` retired same commit; 3 consumers migrated. Sku aggregate at `Domain/Catalog/` to avoid colliding with existing Sku value object; `SetFlashSaleCommandHandler` lifted to its own file per ADV-004 (`e8f4ca6`)
- U7 — `useFilterSearchParams<TSchema>` generic helper on TanStack Router primitives + Inventory + Orders route URL-state migration. Default-omission + filter-or-sort-change-resets-page-+-clears-ledger conventions baked into the hook (D-006). D-005 stale-`?selected=` opens drawer with error panel + Close button (URL not auto-cleared); SkuTable sortable headers + OrdersTable pagination footer — both opt-in (`4fbe874`)
- U8 — Composite UNIQUE on `outbound_saga_transitions (order_id, occurred_at, to_state)` + `SagaTransitionDuplicateInterceptor`. **Plan KTD6 design correction landed during implementation**: EF Core's `SaveChangesFailedAsync` can't actually suppress exceptions (only observe them); primary mechanism shifted to `SavingChangesAsync` pre-check — scan ChangeTracker for Added `OrderTransition` entries → probe DB for matching triple → detach duplicates before save. 23505 never fires in happy redelivery path. Defensive `SaveChangesFailedAsync` remains as log-only for residual concurrent-insert race (`527f32b`)
- U9 — SagaPipeline 8 → 9 nodes (Created + AwaitingReservation un-collapsed on order detail route). OrdersTable cell-level indicator deliberately UNCHANGED per D-007. Vi/En labels via existing `labelsFor` (`86114a0`)
- U6 — Reservation ledger cursor pagination — opaque base64-JSON `(occurredAt, id)` cursor; default page size 50 (was 100), clamps to [1, 200]; backend tuple-style `WHERE (created_at, id) < (cursor.OccurredAt, cursor.Id)` + fetch Limit+1; new composite btree migration `(sku, created_at DESC, id DESC)`. Plan named index `(sku, occurred_at DESC)` but the table has no `occurred_at` column — `created_at` is the existing handler order key; deviation documented inline. Frontend `useSkuLedgerQuery` converted to `useInfiniteQuery`; `<LedgerDrawer>` flattens `data.pages` + Load more button (disabled + aria-busy + spinner) or "All entries loaded" caption. 6 OpaqueCursorTests + 2 new drawer tests; 16/16 drawer total (`648a5b1`)
- U5 — Flash-sale outbox dual-write. `SkuFlashSaleChangedV1` cross-module event mirrors Sprint-5 `StockLevelChangedV1` shape. Inventory's `SkuRepository.UpdateFlashSaleAsync` emits via private `AppendOutbox<T>` helper (mirrors `ReservationRepository`) on state change only — no emit on no-op writes. `AddOutboxRoute<SkuFlashSaleChangedV1>(SendKind.Publish)` registration. StockSync `SkuFlashSaleChangedConsumer` (auto-registered via MT's assembliesToScan) + new `ISkuFlagRepository.ApplyEventAsync(tenantId, sku, isFlashSale, occurredAt, ct)`. **KTD3 OccurredAt guard implemented**: compares `existing.UpdatedAt ?? existing.CreatedAt` against incoming `occurredAt`, rejects stale writes — handles flap A→B→A under W6 competing consumers without per-(tenant, sku) FIFO. CachingSkuFlagRepository wraps with conditional cache eviction (no eviction when stale-rejected). 2 consumer harness tests (`e3f2baf`)
- U4 — `EditSkuModal` 10-field rich edit form + `UpdateSkuCommand` + handler + `PUT /api/v1/inventory/skus/{sku}` endpoint + SkuTable per-row Edit button. Verb PUT matches existing /threshold + /flash-sale convention. Modal: submit-in-progress UX (D-001), barcode 422 → inline `barcode_in_use` error (D-003), dimensions all-or-nothing in mm (D-008), image_url onError fallback (D-009), URL validation. `useEditSkuMutation` follows ULID-per-call + invalidate-on-success + traceId-toast. Edit button opt-in cell-level button (Sprint-7 KTD11); column suppresses when no `onEditClick` (preserves a11y smoke + existing tests) (`a6a579b`)
- U10 — Cross-module index audit. 5 module migrations (Outbound + Channel + Inbound + StockSync + Inventory) each add 2 defensive indexes via `CREATE INDEX IF NOT EXISTS` (idempotent + live-safe) for big-data hot paths — status+created_at composites for list pages, FK-side defensive, time-range diagnostic. Outbox tables excluded (claim+ack semantics already bounded). `CREATE INDEX CONCURRENTLY` deferred per scope (`7773ad7`)
- U11 — This sign-off + README current-stage + CLAUDE.md update + annotated tag `v0.10.1-sprint-7.5`

**Sprint-7.5 deviations from plan file list**:
- **KTD6 design refinement (U8)**: Plan called for "catch DbUpdateException at SaveChanges boundary." EF Core's `SaveChangesFailedAsync` cannot actually suppress exceptions (only observe them). Primary swallow shifted to `SavingChangesAsync` pre-check + detach. Better design than plan called for.
- **U3 namespace**: New Sku aggregate at `ShopFlow.Inventory.Domain.Catalog.Sku` to avoid colliding with existing `Sku` value object. SkuRepository uses `using SkuCode = ShopFlow.Inventory.Domain.Sku;`.
- **U6 index name**: Plan said `(sku, occurred_at DESC)` but table has no `occurred_at` column; named on `created_at` (existing handler order key). Documented in handler docblock + migration comment.
- **Subagent dispatch shape**: Plan KTD9 said 5 rounds of subagent dispatch. U2/U7/U8/U9 shipped via background subagent dispatch; mid-sprint user feedback ("dispatch too slow for me") pivoted U3/U6/U4/U5/U10/U11 to inline execution. Both modes produced commit-quality work; inline is faster when the orchestrator has rich context already in-window.
- **U10 scope refinement**: Plan said "implementer identifies specific indexes via EXPLAIN." Without local Docker, EXPLAIN wasn't possible; U10 writes judgment-based defensive composite + FK + time-range indexes with `CREATE INDEX IF NOT EXISTS` so any pre-existing index is no-op.
- **No local Docker daemon** — same Sprint-1+ posture. Backend integration tests run in CI; frontend `npx tsc --noEmit` + Vitest run locally.

**Sprint-7.5 trade-offs carried forward** (carry into Sprint-8+):
1. Real auth (Sprint-6 trade-off #8) — Sprint-8 still owns password hashing + refresh tokens + role claims + MFA
2. `CREATE INDEX CONCURRENTLY` — revisit before first true production deploy
3. Million-row latency benchmark for AE4 — U6 verified query-plan-uses-index + sub-200ms vs 1k-row fixture; full million-row benchmark deferred to dedicated benchmarking pass
4. Big-data seed loader itself — Sprint-7.5 made the schema + queries + pagination ready; seed loader is later workstream
5. Category enum/autocomplete — ships free-text; revisit when Channel module's product mapping introduces taxonomy (Sprint-9+)
6. Backfilling existing skus rows with rich data — initial migration sets new columns NULL; production import is later
7. 3 pre-existing frontend test failures (axe `empty-table-header` + Response.body re-read) — Sprint-7 baseline; not Sprint-7.5 regressions

**Next implementation step**: cut a fresh branch from `v0.10.1-sprint-7.5` and start one of:
- **Sprint-8 — Real auth module** (password hashing + refresh tokens + role claims + MFA placeholder) — first multi-role surface (picker or ops dispatcher) gates on this
- **Sprint-5.5 — Backend-only scale-gate harness closure** — same shape as Sprint-2.5 / 4.5; runs parallel to any frontend work
- **Phase-3 polish** — observability dashboards (hub connect counts / saga latency / breaker state / flash-sale propagation latency) + Gateway hardening (rate limiting on `/hub/negotiate`) + portfolio README + demo video + deployment docs
- **Big-data seed loader** — the workstream Sprint-7.5's production-readiness was sized for; synthetic million-row reservation ledger + ten-thousand-SKU catalog + ramp-test harness exercising the new indexes + cursor pagination + UNIQUE + OccurredAt guard at real volume
- **New module frontend surface** (Inbound POs UI / Channel mappings UI / Analytics dashboards) — Sprint-7.5 made every future slice inherit camelCase wire + URL-state + cursor pagination + 10-col SKU model from day one

---

**Sprint-7 history** (kept for context; tag `v0.10.0-sprint-7-orders`).

**Sprint-7 Orders Saga Visualisation is complete.** Tag: `v0.10.0-sprint-7-orders`. Sign-off: [`docs/phase-gates/2026-05-19-sprint-7-signoff.md`](./docs/phase-gates/2026-05-19-sprint-7-signoff.md). Plan: [`docs/plans/2026-05-19-001-feat-sprint-7-orders-saga-visualisation-plan.md`](./docs/plans/2026-05-19-001-feat-sprint-7-orders-saga-visualisation-plan.md). Brainstorm: [`docs/brainstorms/2026-05-19-sprint-7-orders-saga-visualisation-requirements.md`](./docs/brainstorms/2026-05-19-sprint-7-orders-saga-visualisation-requirements.md). Ships ShopFlow WMS's **second frontend vertical slice** — Orders saga-visualisation surface at `/orders` (list) + `/orders/$orderId` (detail) with horizontal `<SagaPipeline>` watching the 11-state fulfillment saga, `<TransitionsLog>` newest-first feed with `aria-live="polite"`, and `<OrderLineItems>` (KTD11 cell-level button) reusing Sprint-6's `<LedgerDrawer>` via a `SkuListItem` stub adapter. **Closes Sprint-6 trade-off #9** by lifting SignalR push as shared infrastructure under a **single hub-host topology**: only `Outbound.Api` maps `/hub` + registers the two relay consumers (`StockChangedRelayConsumer` consuming `StockLevelChangedV1`, `SagaTransitionedRelayConsumer` consuming new `SagaTransitionedV1`); Gateway routes `/hub` + `/hub/{**catch-all}` → outbound cluster as the permanent decision (avoids RabbitMQ competing-consumer trap on the eventual W6 split). New `outbound_saga_transitions` per-tenant audit table (module-prefix per Sprint-2.5) written by `SagaTransitionObserver` via explicit per-branch `.ThenAsync(ctx => RecordTransitionAsync(...))` hooks at every `TransitionTo` site in `FulfillmentSaga` — **9 transitions instrumented including the doc-review-flagged hard cases**: the `WhenEnter(CompensatingReservation)` `IfElse` Path A short-circuit branch, the `During(CompensatingReservation).When(StockReleased).If(<=0)` counter-drain branch, and the compound `StockReserved → Reserved → AwaitingPick` chain that produces TWO audit rows in one consume. `SagaTransitionedV1` cross-module integration event carries the W3C TraceContext `CorrelationId` (envelope per AGENTS.md §6.42); audit-row `correlation_id` column was missing from the plan body and added per doc-review coherence fix. **JwtBearer lifted to `AddShopFlowDefaults`** (closes Sprint-6 trade-off #8 *in spirit*; real auth still Sprint-8) + **access-token query-parameter redaction** in `JwtBearerEvents.OnMessageReceived` (doc-review SEC-001 mitigation): copies `?access_token=` to `context.Token` ONLY for `/hub` paths, then rebuilds `QueryString` excluding `access_token` so request-logging middleware never captures the bearer credential. Dev-mode `POST /api/outbound/orders/seed` returns 404 + `environment_not_dev` outside Development; defaults to LineCount=3, clamps to 50, mirrors existing `CreateAsync` outbox-emission shape. New 6 Outbound DTOs + 4 controller endpoints (`GET /`, `GET /kpis`, `GET /{id}/transitions`, `POST /seed`); 7-arg backward-compat ctor keeps Sprint-3-redux's 7 pre-existing OrdersController test files compiling unchanged. Frontend: `@microsoft/signalr ^8.0.7`; `useSignalR` Zustand hook (test-first, 13 scenarios); `useOrdersQuery` + `useOrderMutations` mirror Sprint-6 `useInventoryQuery`/`useInventoryMutations` patterns; `useInventoryQuery` + `useOrdersQuery` toggle `refetchInterval` based on hub state (R13 polling fallback intact per Sprint-6 KTD5). **A11y axe-smoke harness extended** with 4 Sprint-7 cases (SagaPipeline happy + failure, TransitionsLog populated + empty, OrderLineItems). ~351 frontend Vitest tests (+130 from Sprint-6 221); +33 backend unit + 18 backend integration tests. **Doc-review pipeline executed** before start: 5 P1 findings + 2 architectural premise challenges walked through; 1 `safe_auto` fix applied (Auth.Api dropped from `MapShopFlowHubs` target list); 3 P1s declared resolved by orchestrator (correlation_id column, IOutboundOutbox.AppendAsync mechanism, JwtBearer-to-kernel) + 2 routed via user-decision (single-hub-host topology + IStateObserver-via-explicit-wiring). **Subagent dispatch mode shipped**: 14 units across 5 rounds (2 inline + 3 parallel — 5+3+2 subagents); each subagent briefed with plan + the 5 doc-review architectural decisions + reference commit hashes + "do not commit, do not run tests" guard; orchestrator reviewed each diff + committed serially. **15 commits on branch** (1d1c5b9 → tag commit).

**Sprint-7 progress** (as of 2026-05-19):
- ✅ U0 — Branch cut from `v0.9.0-frontend-vertical-slice`; opening commit with brainstorm + plan + 5 doc-review decisions baked into commit body (1d1c5b9)
- ✅ U1 — `outbound_saga_transitions` audit table + `OrderTransition` entity + `IOrderTransitionRepository` + EF entity config + migration `20260519100001_AddOrderTransitions` + 6 integration tests (7be1852)
- ✅ U2 — `SagaTransitionedV1` cross-module contract + `SagaTransitionObserver` class + explicit per-branch `.ThenAsync` hooks at every TransitionTo site in FulfillmentSaga (9 transitions; incl. WhenEnter IfElse Path A + If counter-drain + compound `Reserved → AwaitingPick` chain) + 4 unit + 3 integration tests (b854524)
- ✅ U3 — Outbound MediatR queries — `ListOrdersHandler` / `GetOrderDetailHandler` / `GetOrderTransitionsHandler`; `IOrderRepository.ListAsync` + `GetCurrentSagaStateAsync` extensions; channel-display prefix parser; 16 unit tests (d78bea6)
- ✅ U5 — SharedKernel SignalR foundation — `TenantHub` + `TenantBindingHubFilter` + `MapShopFlowHubs` extension + `AddSignalR` + JwtBearer kernel-lift + access-token query redaction + Auth config propagated to Outbound/Inbound/Channel/StockSync appsettings (4317ba5)
- ✅ U7 — Frontend SignalR client (`signalr.ts`) + `useSignalR` Zustand hook (TEST-FIRST; 13 scenarios) + `@microsoft/signalr ^8.0.7` dep (e79212a)
- ✅ U11 — `<SagaPipeline>` horizontal 8-node component + tokens.css `.saga-step.completed` + `.saga-failure-caption` rules + `aria-current="step"` inside `role="list"` (doc-review design-lens #8); 17 tests (b35ed33)
- ✅ U12 — `<TransitionsLog>` newest-first feed with `aria-live="polite"` (doc-review design-lens #9) + Cancelled-row failure styling; 5 tests (fb79a79)
- ✅ U4 — Outbound `OrdersController` 4 new endpoints + 6 new DTOs + MediatR `assembliesToScan` fix in Program.cs + 9-arg primary ctor + 7-arg backward-compat ctor; 14 integration tests (a59188e)
- ✅ U6 — SignalR relay consumers in SharedKernel.Infrastructure.SignalR/ (registered ONLY on Outbound.Api per single-hub-host); 7 unit + 1 integration tests (507826c)
- ✅ U9 — Inventory `useInventoryQuery` SignalR wire-up — `useStockChangedSubscription` helper + dynamic `refetchInterval` + R13 polling fallback intact; 16 tests (96aecdd)
- ✅ U8 — Frontend Orders API (`api/orders.ts` + 7 DTOs) + 4 query hooks (broad + narrow invalidate) + `useSeedOrderMutation` (TEST-FIRST); 33 tests across 2 files (f1bf866)
- ✅ U10 — `/orders` list route + `OrdersKpiStrip` + `OrdersFilterStrip` + `OrdersTable` (KTD11) + `SeedTestOrderButton` (DEV-only) + Sidebar rename outbound→orders + outbound.tsx soft-deleted via beforeLoad redirect; 8 tests (8c00555)
- ✅ U13 — `/orders/$orderId` detail route + `<OrderLineItems>` (KTD11) + LedgerDrawer reuse via SkuListItem stub + `inferFailureCause` helper; 5 tests (8b3ee6b)
- ✅ U14 — a11y smoke harness extended (4 new cases) + [Sprint-7 sign-off](./docs/phase-gates/2026-05-19-sprint-7-signoff.md) + CHANGELOG entry + README + CLAUDE.md update + tag `v0.10.0-sprint-7-orders`

**Sprint-7 deviations from plan file list** (full detail in sign-off Key Technical Decisions + Deviations sections):
- **KTD3 — IStateObserver shipped as explicit per-branch `.ThenAsync` wiring**, not MT's `IStateObserver<TInstance>` interface. MT 8.3.4 doesn't reliably expose the interface through `MassTransitStateMachine<T>`; backend tests run only in CI so the interface mechanism couldn't be iterated locally. Class-shape decision preserved (single observer class, comprehensive branch coverage including WhenEnter/IfElse/If branches).
- **KTD2 — Single hub-host topology** (doc-review decision): only Outbound.Api maps `/hub` + registers relay consumers; Gateway routes `/hub` there permanently. Other modules don't host the hub. Auth.Api also excluded (intentionally skips AddShopFlowDefaults so AddSignalR isn't wired; safe_auto doc-review fix corrected the plan).
- **KTD4 — `correlation_id` column added to `outbound_saga_transitions`** (doc-review coherence fix). Origin R14 specified it but plan U1 schema omitted it. Schema now: `id`, `order_id`, `from_state`, `to_state`, `occurred_at`, `event_type`, `correlation_id`, `created_at`, `updated_at`.
- **KTD5 — `IOutboundOutbox.AppendAsync` as the audit-event publish mechanism** (doc-review feasibility fix). Plan said "existing `AppendOutbox<T>` helper on `OutboundDbContext`" but no such helper exists; Outbound's actual outbox surface is `IOutboundOutbox.AppendAsync(string eventType, object payload, ct)`.
- **KTD6 — JwtBearer lifted to kernel + access-token query redaction** (doc-review SEC-001 + Sprint-6 trade-off #8 close in spirit). Real auth (password hashing + refresh + role claims + MFA) still Sprint-8.
- **KTD7 — Subagent dispatch shape**: 5 rounds total. Round 1 fired 5 parallel (U3/U5/U7/U11/U12 — file-disjoint); Round 2 fired 3 (U4/U6/U9); Round 3 single (U8 — gates Round 4); Round 4 fired 2 (U10/U13). Shared-directory mode (worktree isolation skipped since `.claude/worktrees/` not yet gitignored).
- **KTD8 — `OrdersController` 9-arg primary + 7-arg backward-compat ctor**: keeps Sprint-3-redux's 7 pre-existing test files compiling unchanged. Avoids ballooning the diff into legacy test code.
- **KTD9 — `RecordTransitionAsync` uses `GetService<>` (nullable)** rather than `GetRequiredService<>`. Sprint-3-redux's `FulfillmentSagaTests` builds the MT TestHarness without registering the Sprint-7 observer chain; re-wiring every legacy test would have rippled. Production registration via `AddOutboundModule` is load-bearing; `SagaTransitionsAuditFlowTests` integration test catches the silent-failure risk.
- **KTD10 — `outbound.tsx` rewritten as `beforeLoad` redirect to `/orders`** (no Delete tool available to the subagent). Bonus: stale bookmarks degrade gracefully.
- **KTD11 — `LedgerDrawer` adapter pattern** at Orders detail: drawer's prop is `item: SkuListItem | null`; detail route builds a minimal `SkuListItem` stub (Allocations: [], IsFlashSale: false) from clicked line's SKU. Drawer's internal `useSkuLedgerQuery(sku)` is the real data source.
- **KTD12 — Channel display parsed from `Order.ChannelExternalOrderId` prefix** (`SHOPEE_*` → "Shopee", `LAZADA_*` → "Lazada", `TIKTOK_*` → "TikTok Shop", else "Direct"). No new `ChannelType` column on `orders`.
- **No local Docker daemon on this dev machine** — same Sprint-1+ posture. Backend integration tests run in CI; field-shape drift on contract types referenced in U2's integration tests will surface there if it exists.
- **Backend builds run in CI only** — .NET 8.0.407 vs the repo's `global.json`-pinned 9.0.305.

**Sprint-7 trade-offs locked in** (carry into Sprint-7.5+; full list in sign-off):
1. No Postgres `UNIQUE` constraint on `outbound_saga_transitions` — double-audit-write under MT redelivery accepted; UNIQUE on `(order_id, occurred_at, to_state)` is a Sprint-7.5 follow-up if observed.
2. No `ChannelType` column on `orders` — display parsed from `ChannelExternalOrderId` prefix.
3. Cross-module joins (channel allocations + p24 outbound) still empty (Sprint-6 trade-off #3 unchanged).
4. URL-search-params persistence still local React state (Sprint-6 trade-off #4 unchanged); Sprint-7.5 candidate.
5. camelCase wire normalisation still deferred (Sprint-6 trade-off #6); new Sprint-7 endpoints ship PascalCase consistent with KTD4.
6. Reservation ledger cursor pagination still deferred (Sprint-6 trade-off #5).
7. Cosmetic SKU schema expansion still in `InMemorySkuMetadataStore` singleton (Sprint-6 trade-off #1); Sprint-7.5 candidate.
8. Flash-sale dual-write to StockSync still single-endpoint (Sprint-6 trade-off #10); Sprint-7.5 candidate.
9. Real auth (Sprint-6 trade-off #8) — JwtBearer lifted to kernel + access_token redaction in place, but the dev-mode baked JWT remains. Real password hashing + refresh + role claims + MFA is Sprint-8.
10. Polling fallback intact (R13 satisfied; Sprint-6 KTD5 preserved): `useInventoryQuery` + `useOrdersQuery` toggle `refetchInterval` based on hub state.
11. SagaPipeline `Created` + `AwaitingReservation` collapse into "Placed" (doc-review adversarial #7 advisory): UX call accepted; 9-node split is a Sprint-7.5 candidate if operations want to see slow Inventory consumers.
12. `SagaTransitionedV1` outbox-routed Publish (doc-review adversarial #5 advisory): accepted for parity with `stock_changed`; revisit if AE3's "~200ms hub event" target ever drifts.
13. Subagent dispatch in shared-directory mode (`.claude/worktrees/` not yet gitignored; worktree isolation deferred).

**Next implementation step**: cut a fresh branch from `v0.10.0-sprint-7-orders` and start one of:
- **Sprint-7.5** — Bundled trade-off closures: cosmetic SKU schema expansion + camelCase wire normalisation + flash-sale dual-write + URL-search-params persistence + reservation ledger cursor pagination + optional UNIQUE on `outbound_saga_transitions`. ~1-week point release matching Sprint-2.5 / 4.5 / 5.5 cadence.
- **Sprint-8** — Real auth module (password hashing + refresh tokens + role claims + MFA placeholder) replacing the dev-mode baked JWT; first multi-role surface (picker or ops dispatcher).
- **Sprint-5.5** — Backend-only scale-gate harness closure. Runs parallel to any Sprint-7.5/8 frontend work.
- **Phase-3 polish** — Observability dashboards (hub connect counts / saga latency / breaker state), Gateway hardening (rate limiting on `/hub/negotiate`), portfolio README + demo video.
- **Public blog post derivative** — adapted ~3000-4000 word version of `docs/methodology.md` OR a Sprint-6/7 frontend case study.

---

**Sprint-6 history** (kept for context; tag `v0.9.0-frontend-vertical-slice`). Sign-off: [`docs/phase-gates/2026-05-19-sprint-6-signoff.md`](./docs/phase-gates/2026-05-19-sprint-6-signoff.md). Ships ShopFlow WMS's **first frontend surface** — new top-level `web/` subdirectory with Vite 5 + React 19 + TypeScript strict + TanStack Router (file-based) + TanStack Query (2-s polling) + Zustand. Inventory screen × Owner role end-to-end through real `Inventory.Api` WRITE controllers; 8 other screens shipped as `ComingSoon` placeholders behind the auth guard so the navigation shell was complete on day one. New stub-grade `ShopFlow.Auth.{Domain,Application,Infrastructure,Api}` 4-csproj quartet returns a baked JWT (Sprint-7 lifts JwtBearer to kernel + access-token redaction). Owner write paths: `<AdjustStockModal>` (delta + reason + note), `<ThresholdInlineEdit>` (optimistic UI via React 19 set-state-during-render pattern; **KTD10**), `<FlashSaleToggle>` (Inventory.Api endpoint per Sprint-6 trade-off #3; not StockSync), `<CreateSkuModal>` (sku regex + 409 → inline duplicate error; collects only `sku + initialAvailable` per trade-off #1). Reusable primitives: `<Drawer>` + `<Modal>` (Modal Esc capture-phase + stopImmediatePropagation so Modal-over-Drawer coexists; **KTD9**), `<Toast>` + `useToast` Zustand global queue, `<Toggle>` (role=switch), `<AllocationBar>` + `<LedgerRow>` + `<LedgerDrawer>`. `useInventoryMutations` test-first: ULID-per-call Idempotency-Key (audit-only dedup per trade-off #2). **CI + a11y harness**: new `frontend` job in `.github/workflows/ci.yml`; **KTD11** (emergent in U13): axe `nested-interactive` violation caught — SkuTable refactored so row drops button semantics. **KTD12**: vitest-axe@0.1.0 type-augmentation shim (`web/src/types/vitest-axe.d.ts`). 221 frontend Vitest tests green; +2 backend Auth.UnitTests = 361 total backend. Bundle: vendors 311.16 kB (gz 97.93 kB). Vercel agent-skills applied during U11-U13.

**Sprint-6 trade-offs locked in** (Sprint-7 closes #8 in spirit via JwtBearer kernel-lift + access-token redaction, #9 via SignalR push, and #3 partially via SagaPipeline reading the cross-module audit table; #1/#4/#5/#6/#10 still deferred to Sprint-7.5+):
1. No new schema migration in Sprint-6 (cosmetic columns wait for Sprint-7.5); threshold + flash-sale in `InMemorySkuMetadataStore` singleton.
2. No `inventory_idempotency_records` table — header logged, natural dedupe via `stock_adjustments` audit.
3. No cross-module joins — channel allocations + p24 outbound ship empty in Sprint-6 list. (Sprint-7 SagaPipeline reads from the new Outbound audit table without crossing module DbContexts; the trade-off itself is unchanged.)
4. No URL-search-params persistence for filter state.
5. Reservation ledger cursor pagination deferred.
6. Wire shape PascalCase (.NET default serializer); Sprint-7.5 normalisation deferred.
7. Backend builds run in CI, not on this dev machine.
8. Fake auth (Sprint-7 lifted JwtBearer to kernel + added access-token redaction; real password hashing + refresh + MFA still Sprint-8).
9. 2-s polling (Sprint-7 closed via SignalR push + R13 polling fallback retained).
10. Flash-sale single-endpoint (Sprint-7.5 will dual-write to StockSync).

---

**Phase-3 Methodology Writeup history** (kept for context; tag `v0.8.0-methodology-writeup`). Sign-off: [`docs/phase-gates/2026-05-18-methodology-writeup-signoff.md`](./docs/phase-gates/2026-05-18-methodology-writeup-signoff.md). Ships [`docs/methodology.md`](./docs/methodology.md) (~8500 words) — comprehensive 7-sprint chronological case study + synthesis pattern catalog + friction section documenting the AI-assisted development methodology used to build ShopFlow WMS. Audience: future-self + developers cloning the repo (not HR/recruiter). 100% in `docs/`; no source code changes. Seven units U1-U7: skeleton + lead-in + reference inventory; Phase-0-redux + Sprint-1-redux sections; Sprint-2-redux + 2.5 + 3-redux sections; Sprint-4 + 4.5 sections; Sprint-5 section (most-detailed); synthesis pattern catalog (5 patterns, 2 Mermaid diagrams); friction section (7 named modes) + forward-looking section + wrap-up. Closing: "one project, one solo dev, one stack — read this as evidence, not prescription".

---

**Sprint-5 history** (kept for context; tag `v0.7.0-sprint-5`). Sign-off: [`docs/phase-gates/2026-05-17-sprint-5-signoff.md`](./docs/phase-gates/2026-05-17-sprint-5-signoff.md). **Closes Phase-2 egress with a new `ShopFlow.StockSync` module** (7th logical module in the modular monolith) consuming Inventory's stock-mutation outbox stream and pushing per-channel `available_to_sell` updates through a four-layer isolation pipeline: coalescing buffer per `(tenant, sku, channel)` (singleton `ConcurrentDictionary` with `AddOrUpdate` last-by-`ObservedAt` tiebreaker; `PeriodicTimer(CoalesceWindowMs)` flush) → per-tenant priority queue (paired bounded `Channel<PushIntent>` lanes, `DropOldest`, strict-priority `ReadNextAsync`) → token bucket per `(tenant, channel)` (built-in `System.Threading.RateLimiting.TokenBucketRateLimiter`; `IDisposable` registry) → Polly v8 circuit breaker per `(tenant, channel)` (`ResiliencePipeline<Result>` with `CircuitBreakerStateProvider` for diagnostics; up-front gate vs mid-execute `BrokenCircuitException` distinguished in audit) → `IChannelAdapter.PushStockUpdateAsync` (Sprint-4 stub filled with real HTTP POST + status-code → stable error-code mapping; Shopee mock gains `/api/v2/product/update_stock` + `IsStockUpdateChaosActive` toggle) → `stock_sync_push_log` audit row (UNIQUE-23505 idempotent on `idempotency_key`). **KTD1**: replaces literal brainstorm R1 (consume 3 transition events `StockReservedV1`/`StockReleasedV1`/`StockConfirmedV1`) with single canonical `StockLevelChangedV1(TenantId, Sku, AvailableToSell, OccurredAt)` event — `StockReleasedV1` only carries `OrderLineIds`, `StockConfirmedV1` has no per-line detail; coupling StockSync to Outbound for line→SKU mapping was race-prone. Inventory's pre-existing `StockChangedEvent` domain event already had the right intent. Inventory emits `StockLevelChangedV1` from 5 stock-mutating repository paths (TryReserve/Confirm/Release/ReleaseLines/ReleaseExpired) + `StockItemRepository.AdjustAtBinAsync`. **KTD7 (emerged in U7)**: `ISkuFlagRepository` port takes explicit `Guid tenantId` — consumer + dispatcher call from singleton context without ambient `RequestContext`; cache key + scope binding both need explicit tenant. The `CachingSkuFlagRepository` singleton wrapper opens a DI scope + binds `RequestContext` via `ITenantCatalog.LookupByIdAsync` + K12 pattern before delegating to the scoped inner repo. Full Api composition: `AddStockSyncModule` registers all ports + impls + 3 hosted services (`CoalesceFlushService`, `PerTenantDispatcherService`, `MultiplexedOutboxDispatcher<StockSyncDbContext>`); Aspire AppHost adds `stocksync-api`; Gateway routes `/api/sync/**` + `/api/skus/**`. 359 unit tests green (+71 from Sprint-5: 14 U1 + 19 U3 + 11 U4 + 10 U5 + 9 U6 + 8 U7). +24 `Category=Integration` tests (6 U2 + 4 U5 + 7 U7 + 1 controller Skip + 3 U8 + 2 U6 + 1 U9). +2 `Category=Load` scale-gate slots Skip'd per Sprint-4 U9 precedent. CI runs the full Integration + Load suite; dev-machine Docker daemon deferred per Sprint-1/3/4/4.5 precedent.

**Sprint-5 progress** (as of 2026-05-17):
- ✅ U1 — module quartet scaffold + `StockSyncDbContext` + 3 tables + `InitialStockSyncSchema` migration + `SkuFlag` + `PushLogEntry` aggregates + 14 unit tests + 6th smoke test method + `ShopFlow.sln` updated
- ✅ U2 — `StockLevelChangedV1` contract (KTD1) + Inventory emit in `ReservationRepository` (5 paths) + `StockItemRepository.AdjustAtBinAsync` + generic `AppendOutbox<T>` + `ReadAvailableForSkusAsync` helper for CTE paths + 6 integration tests
- ✅ U3 — `CoalescingBuffer` (singleton ConcurrentDictionary, last-by-ObservedAt tiebreaker), `StockLevelChangedConsumer` (per-channel fanout, IsFlashSale stamping), `CoalesceFlushService` (PeriodicTimer), `PushIntent` + `BuildIdempotencyKey`, `IPerTenantQueue` port, `StockSyncOptions`; 19 unit tests
- ✅ U4 — `PerTenantQueue` (paired bounded Channel lanes, strict-priority ReadNextAsync), `TenantChannelBucketRegistry` (IDisposable; TokenBucketRateLimiter), `StockSyncOptions.TokenBucket` + `QueueCapacity` settings; 11 unit tests
- ✅ U5 — `PushPipelineFactory` (ResiliencePipeline<Result> + CircuitBreakerStateProvider), `TenantChannelBreakerRegistry` (GetOrAdd + GetState diagnostics), `PerTenantDispatcherService` (per-tenant Task.Run loop), `IPushLogRepository` + `PushLogRepository` (UNIQUE-23505 idempotent), `StockSyncOptions.Breaker`; 10 unit + 4 integration tests
- ✅ U6 — `ShopeeAdapter.PushStockUpdateAsync` body (real HTTP POST, HttpRequestMessage rebuilt per retry, snake_case payload via dedicated ShopeeJson.Options); Shopee mock `/api/v2/product/update_stock` endpoint + `IsStockUpdateChaosActive`; namespaced `MockEntryPoint`; 9 adapter unit + 2 mock round-trip integration tests
- ✅ U7 — `SkuFlagRepository` (scoped EF inner, UNIQUE-23505 catch + idempotent upsert), `CachingSkuFlagRepository` (singleton wrapper with K12 scope binding), `SkuFlagsController`; KTD7 `ISkuFlagRepository` port signature change (explicit tenantId); 8 cache unit + 7 integration + 1 Skip'd controller test
- ✅ U8 — `AddStockSyncModule` composition extension, `ChannelLookupPort` singleton, full `Program.cs`, `SyncStateController` diagnostics, Aspire `AddProject<>`, Gateway routes; 3 composition integration tests
- ✅ U9 — `StockSyncHappyPathTests` (end-to-end via WebApplicationFactory<Program> + InMemory MT + FakeChannelAdapterFactory recorder), `TenantBurstDriver` + `FairnessCalculator` helpers, `MultiTenantStockSyncScaleGateTests` (2 Skip'd Category=Load slots per Sprint-4 U9 precedent)
- ✅ U10 — [Sprint-5 sign-off](./docs/phase-gates/2026-05-17-sprint-5-signoff.md); CHANGELOG entry; README + CLAUDE.md update; tag `v0.7.0-sprint-5`

**Next implementation step**: cut a fresh branch from `v0.7.0-sprint-5` and start one of:
- **Sprint-5.5** — close the U9 scale-gate harness gap (multi-tenant Aspire boot + real Shopee mock alongside StockSync.Api so wall-time noisy-neighbor + breaker-recovery measurements land). Same posture as Sprint-4 → Sprint-4.5.
- **Sprint-6** — Analytics module (W9-W10): read-side projections / dashboards consuming the existing outbox stream including the new `StockLevelChangedV1`.
- **Phase-3 polish** — Gateway hardening (auth middleware, rate limiting), observability dashboards (Prometheus + Grafana for reservation latency / push latency / breaker state / fairness floor), portfolio README + demo video, deployment docs.

**Sprint-5 deviations from plan file list**:
- **KTD1 — single `StockLevelChangedV1` event** replaces literal R1's 3-event consume. Documented in plan + sign-off as a Key Technical Decision (the brainstorm interview did not surface that `StockReleasedV1` carries only `OrderLineIds` and `StockConfirmedV1` has no per-line detail; reading the actual contract definitions during plan-time triggered the swap).
- **KTD7 — `ISkuFlagRepository` port signature change** to take explicit `Guid tenantId`. Emerged in U7 when the singleton wrapper's scope-binding analysis revealed the per-tenant cache key needed explicit tenant. Consumer + 4 NSubstitute call sites in U3 unit tests updated.
- **U2 — `AppendOutbox<T>` generic helper added in TWO repositories** (ReservationRepository + StockItemRepository) instead of one. The layering (Inventory's two repos in different files, both writing outbox rows) made code-sharing via static helper awkward.
- **U3 — `IPerTenantQueue.ReadNextAsync` deferred to U4** because U3 only writes to the queue; U4 owns the queue impl + reader semantics. Plan implied both lived in U3.
- **U4 — `PerTenant` config override on `CoalesceWindowMs`** dropped from plan (no test, no usage). The per-tenant override only makes sense once U4's per-tenant dispatcher exists, and at U4 there's still no use case driving the parameter.
- **U5 — `PerTenantDispatcherService` enumerates tenants once on startup** (not on tenant-added events). Phase-3 work; documented in code remarks.
- **U5 — `StockUpdateRequest.ChannelId = Guid.Empty`** routes by channel TYPE only via `IChannelAdapterFactory.ResolveFor`; per-tenant `ChannelId` lookup is Phase-3 when StockSync queries Channel module's `channels` table.
- **U6 — `HttpRequestMessage` rebuilt per retry attempt** because single-shot send semantics throw on re-use (Sprint-3-redux `MockShippingProvider` precedent).
- **U6 — Mock chaos state is process-wide singleton** (not per-tenant). Sprint-4 U9 → Sprint-4.5 same precedent. Per-tenant chaos is Phase-3.
- **U6 — `X-ShopFlow-Idempotency-Key` is internal audit header**, not a real Shopee API contract. Phase-3 either drops or translates.
- **U7 — `SkuFlagsController` integration test placeholder Skip'd** because U7's Program.cs is still the U1 stub. U8 ships the real Program.cs; U8 composition test covers controller boot.
- **U7 — Crude LRU eviction** at 10k entries (one arbitrary key dropped per overflow) instead of proper touch-on-read LRU. Phase-3 upgrade.
- **U8 — `MultiplexedOutboxDispatcher<StockSyncDbContext>` registered** even though StockSync doesn't emit cross-module events yet. Infrastructure parity; Phase-3 events drop in without DI shuffle.
- **U8 — Gateway uses two route entries** (`/api/sync/**` + `/api/skus/**`) pointing at one cluster. Channel uses one route; StockSync owns two URL prefixes.
- **U8 — Composition test uses `MessageBus:Transport=InMemory`** so RabbitMQ is not required for the boot. U9 happy-path uses the same approach.
- **U9 — Happy-path uses `FakeChannelAdapterFactory` recorder** instead of booting the out-of-process Shopee mock alongside StockSync.Api. The StockSync project graph doesn't reference Channel.Infrastructure (W6-split intent); the real adapter + mock round-trip is exercised by U6 `ShopeeMockRoundTripTests`. The fake records the exact `StockUpdateRequest` the dispatcher hands the adapter — that *is* the assertion the plan's "mock received push" check translates to, scoped to the dispatcher's contract surface.
- **U9 — 2 scale-gate slots ship Skip'd** per Sprint-4 U9 precedent. Wall-time noisy-neighbor + breaker-recovery measurements deferred to Sprint-5.5.
- **No local Docker daemon on this dev machine** — same Sprint-1-redux + Sprint-3-redux + Sprint-4 + Sprint-4.5 posture. Integration + Load tests run in CI.

---

**Sprint-4.5 history** (kept for context; tag `v0.6.1-sprint-4.5`). Sign-off: [`docs/phase-gates/2026-05-15-sprint-4.5-signoff.md`](./docs/phase-gates/2026-05-15-sprint-4.5-signoff.md). **Closes the four Sprint-4 deferrals as a ~1-week point release.** The Channel receiver now: routes through `IChannelAdapterFactory.ResolveFor(channelType).ParseWebhook(...)` for the real marketplace-asserted `provider_event_id` (body-hash stub deleted); `WebhookOrchestrator` gates on `event_type == "order.created"`, resolves per-line external→internal SKUs via `IProductMappingService`, builds + emits the canonical `OrderImportedV1` (or **fails the whole import** per the `OrderImportedLineV1` contract canon when any line is unmapped — brainstorm R6 reversal documented in plan + sign-off); `IngestWebhookService.IngestFailedAsync` persists `webhook_events.status=Failed` rows without outbox writes, UNIQUE-23505 idempotency preserved; receiver returns 200 with `{ status: "order_imported" | "import_failed" | "no_downstream", ... }` shape mapping. `TenantWebhookHarness` integration helper uses `WebApplicationFactory<Program>` + multi-tenant Testcontainers Postgres provisioning + per-tenant signing-secret seeding. Three `Category=Load` scale-gate bodies in place: burst-200rps × 5 tenants × 5s (p99 < 200ms + fairness ≥ 0.85), replay-100× with fixed `event_id` → exactly 1 outbox row, cross-tenant signature → 401 + zero DB writes. 288 unit tests green (+19 from Sprint-4.5: 13 ShopeeAdapterParseOrderCreated + 7 WebhookOrchestrator). 1 new `Category=Integration` smoke test exercises the harness end-to-end. CI runs the full Integration + Load suite; dev-machine Docker daemon deferred per Sprint-1/3/4 precedent.

**Sprint-4.5 progress** (as of 2026-05-15):
- ✅ U1 — `IChannelAdapter.ParseOrderCreated(envelope) → Result<ExternalOrderDraft>` + `ShopeeAdapter` impl reading the real Shopee Open Platform v2 shape (`data.ordersn`, `data.items[].item_sku`, `data.items[].model_quantity_purchased`, `data.package_list[0].shipping_carrier`); `ShopeeWebhookParser.ParseOrderData(rawPayload)`; 13 unit tests with stable error codes (`shopee.order.*`)
- ✅ U2 — `WebhooksController` routes through `IChannelAdapterFactory.TryResolve(channelType).ParseWebhook(...)`; `ExtractProviderEventIdStub` deleted; unknown channel-type → 501; parse failures → 400 + stable code; `BuildHeaderSnapshot()` helper
- ✅ U3 — `WebhookOrchestrator` (new Application service); event-type gating + per-line `IProductMappingService.ResolveAsync` + **fail-whole-import on any unmapped line** (canon-correct vs brainstorm R6); `IngestWebhookService.IngestFailedAsync` failed-path entry; controller maps `WebhookProcessStatus` → 200-shape responses; 7 NSubstitute unit tests
- ✅ U4 — `tests/ShopFlow.Channel.IntegrationTests/Harness/`: `ChannelWebhookFixture` (xUnit collection), `TenantWebhookHarness` (provisioning + `WebApplicationFactory<Program>` + signed-POST), `SignedWebhookSender` (HMAC-SHA256 mirror of `ShopeeSigner`); 1 `Category=Integration` smoke test
- ✅ U5 — 3 `Category=Load` scale-gate bodies (burst + replay + cross-tenant signature) + `FairnessCalculator`; `SendAsync(eventId?)` knob added to the harness so replay tests force same `provider_event_id`
- ✅ U6 — [Sprint-4.5 sign-off](./docs/phase-gates/2026-05-15-sprint-4.5-signoff.md); CHANGELOG entry; README + CLAUDE.md update; tag `v0.6.1-sprint-4.5`

**Next implementation step**: cut a fresh branch from `v0.6.1-sprint-4.5` and start **Sprint-5 Stock Sync Engine** (Phase-2 W6-W8 centerpiece per product plan v3.0 §9.4): coalescing buffer per `(tenant, sku, channel)` + per-channel token bucket per tenant + flash-sale priority queue + Polly circuit breaker per `(tenant, channel)` + allocation engine. The Sprint-5 scale gate is the headline noisy-neighbor test (5 tenants concurrently, tenant A bursts 2k stock changes/s for 5min, B-E maintain p99 < 30s, per-tenant fairness floor ≥ 0.85). Plan TBD.

**Sprint-4.5 deviations from plan file list**:
- **U1 field names corrected to actual Shopee fixture shape** — the plan referenced idealized `{order_sn, item_list, quantity}` field names; the real fixture at `tests/fixtures/channels/shopee/webhook-order-created.json` (and the prior Shopee documentation it derives from) uses `{ordersn, items, model_quantity_purchased}`. U1 reads the real shape; documented in U1 commit message.
- **U3 R6 reversal** — brainstorm proposed emit-with-`InternalSku=null`; `OrderImportedLineV1.Sku` is non-nullable AND contract docs explicitly mandate fail-whole-import. Plan + sign-off document the reversal as a Key Technical Decision.
- **U3 sentinel skip-event** — non-`order.created` events persist via a sentinel `WebhookEventSkippedV1` event type that the `OutboxRouteRegistry` treats as `PublishDefault` with no subscriber (no-op at broker). Sprint-6+ replaces with explicit per-event-type policy.
- **U5 replay test required harness extension** — adding `eventId` optional parameter to `SendAsync` lets the replay test force same `provider_event_id` across all 100 sends; without it the harness auto-generates a fresh GUID per call.
- **No Postgres / integration / runtime smoke on this dev machine** (Docker daemon not running) — same Sprint-1-redux + Sprint-3-redux + Sprint-4 posture. Integration + Load tests run in CI.

---

**Sprint-4 history** (kept for context; tag `v0.6.0-sprint-4`). Sign-off: [`docs/phase-gates/2026-05-13-sprint-4-signoff.md`](./docs/phase-gates/2026-05-13-sprint-4-signoff.md). **Opens Phase-2's channel-ingress half.** The Channel module ships: 3 Domain aggregates (Channel, WebhookEvent, ProductMapping) + value objects, full ChannelDbContext + InitialChannelSchema migration (4 tables; `UNIQUE(channel_id, provider_event_id)` webhook idempotency anchor), webhook receiver pipeline (ShopeeSignatureVerifier HMAC-SHA256 + `FixedTimeEquals`, WebhookEventRepository UNIQUE-23505 catch mirroring Sprint-1-redux, IngestWebhookService first-write-only outbox append, `[SkipTenantRouting]` + middleware mod, WebhooksController), **K13 close** (`IOutboxRouteRegistry` + `services.AddOutboxRoute<T>(...)`; dispatcher branches Send vs Publish; unregistered → `OutboxRoute.PublishDefault` — Sprint-1/2/3 unchanged), `IChannelAdapter` framework + Shopee adapter + parser, three-tier product mapping engine (Exact → Levenshtein → null), separate-process Shopee mock server at `tools/mocks/shopee/` wired into Aspire via `AddProject<>`, `OrderImportedV1` cross-module contract + Outbound consumer (idempotent on `Order.ChannelExternalOrderId` UNIQUE), Channel.Api Program.cs fully composed (AddShopFlowDefaults → AddControlPlane → AddChannelModule → UseTenantRouting). 269 unit tests green (+30 from Sprint-4); Channel.IntegrationTests skeleton + MultiTenantWebhookScaleGateTests with 3 Skip'd `Category=Load` slots (harness body deferred to Sprint-4.5 follow-up).

**Sprint-4 progress** (as of 2026-05-13):
- ✅ U1 — Channel Domain (Channel, WebhookEvent, ProductMapping aggregates + 4 value objects + 3 enums + 32 unit tests)
- ✅ U2 — ChannelDbContext + 4 entity configs + InitialChannelSchema migration (channels / webhook_events / product_mappings / channel_outbox_messages — Sprint-2.5 per-module prefix) + MigrationSmokeTests 5th method
- ✅ U3 — Webhook receiver pipeline + UNIQUE-23505 idempotency + `[SkipTenantRouting]` attribute + TenantRoutingMiddleware honors it (endpoint metadata check); WebhooksController returns 404/401/200; 18 new unit tests
- ✅ U4 — K13 close: IOutboxRouteRegistry + OutboxRouteRegistry (DI-seed enumeration, last-write-wins) + OutboxRoute + SendKind + OutboxRouteSeed + services.AddOutboxRoute<T>(SendKind, destination?) + MultiplexedOutboxDispatcher.DispatchOneTenantAsync reads registry per row, branches to ISendEndpointProvider for Send-kind, default destination = kebab-case CLR type name; 8 OutboxRouteRegistryTests
- ✅ U5 — IChannelAdapter + IChannelAdapterFactory + ChannelAdapterFactory + ShopeeAdapter (stateless; PushStockUpdate Sprint-5 stub) + ShopeeWebhookParser + ChannelServiceCollectionExtensions.AddChannelModule (Polly v8 retry pipeline + typed HttpClient); 13 new tests
- ✅ U6 — IProductMappingRepository + IProductMappingService + ProductMappingRepository (UNIQUE-23505 manual upsert) + HybridProductMappingService (Exact → in-process Levenshtein @ 0.6 → null) + ProductMappingsController; 8 hybrid-service tests
- ✅ U7 — Shopee mock server (separate Kestrel process at `tools/mocks/shopee/`; ShopeeSigner intentionally independent from verifier to surface drift; SecretRegistry + ChaosState + POST /__chaos / /__send-webhook / /__seed-channel endpoints); Aspire AppHost AddProject<Projects.ShopFlow_Mocks_Shopee>("shopee-mock") replaces U9 placeholder
- ✅ U8 — ShopFlow.Contracts.Channel.OrderImportedV1 + OrderImportedLineV1; OrderImportedConsumer in Outbound.Application (idempotent on Order.ChannelExternalOrderId UNIQUE, reuses Sprint-3 ports, enqueues canonical OrderPlacedV1 outbox row); AddOutboxRoute<OrderImportedV1>(SendKind.Send) in Channel module; 3 consumer tests via MT TestHarness + NSubstitute
- ✅ U9 — Channel.Api Program.cs full composition + appsettings dev defaults (ConnectionStrings:rabbitmq, MessageBus:Transport=RabbitMq, ControlPlane:ConnectionString + TenantTemplate, Channel:Shopee:MockBaseUrl); ShopFlow.Channel.IntegrationTests project skeleton + MultiTenantWebhookScaleGateTests with 3 Skip'd Category=Load slots
- ✅ U10 — [Sprint-4 sign-off](./docs/phase-gates/2026-05-13-sprint-4-signoff.md); CHANGELOG entry; README + CLAUDE update; tag `v0.6.0-sprint-4`

**Sprint-4 deviations from plan file list**:
- **U9 scale-gate harness body deferred** — class shell ships with 3 `Fact(Skip=…)` slots; supporting harness is Sprint-4.5 follow-up. Same precedent as Sprint-1-redux U5 + Sprint-3 U8 (wall-time measurement deferred to nightly CI).
- **WebhooksController provider_event_id stub** — derives idempotency token from `(body, signature)` hash. U5 parser is in DI but controller not yet wired to it. UNIQUE catches replay either way.
- **`OrderImportedV1` not yet emitted by receiver** — contract + consumer + Send-routing ready; receiver writes placeholder event-type string. Sprint-4.5 wires receiver → parse → resolve SKUs → emit.
- **No Postgres / integration / runtime smoke on this dev machine** (Docker daemon not running) — same Sprint-1-redux + Sprint-3-redux posture. Integration tests including ChannelMigration_AppliesAndLeavesNamedObjects deferred to CI.
- **`Microsoft.Extensions.Http` 10.0.7 added to CPM** for ShopeeAdapter typed HttpClient (aligns with Microsoft.Extensions.Hosting floor).

---

**Sprint-3-redux history** (kept for context; tag `v0.5.0-sprint-3-redux`). **Closed Phase-1's customer funnel** — Inventory holds stock (Sprint-1-redux), Inbound fills it (Sprint-2-redux), Outbound drains it (Sprint-3-redux). The Outbound module ships the full fulfillment saga (MassTransit state machine, 11 states, EF saga repository with K12 per-tenant DbContext binding), 9 cross-module contracts, 3 new Inventory consumers wrapping the extended `ReservationRepository` (`TryReserveLinesAsync` + `ReleaseLinesAsync`), `IPickQueue` per-tenant `Channel<PickRequestV1>` + `PickWaveGeneratorService`, mocked shipping carrier with Polly v8 retry, pick-failure compensation, and the W5 scale gate (operator-pipeline measurement). K11 multi-row CTE concurrency fix landed as institutional learning. K15 MT.EFCore 8.3.4 + EF Core 9 binding verified.

**Sprint-3-redux progress** (as of 2026-05-13):
- ✅ U1 — Outbound module quartet scaffold + `InitialOutboundSchema` 7-table migration + K15 MT.EFCore 8.3.4 + EF Core 9 smoke build PASS
- ✅ U2 — `Order` + `OrderLine` aggregate + repository + `IUnitOfWork` + `IOutboundOutbox` + idempotent `POST /orders` + `GET /orders/{id}` (29 Order unit + 5 OrderRepository + 8 OrdersController integration tests)
- ✅ U3 — Inventory schema extension (`reservations_ledger` + `order_line_id` + composite UNIQUE); `IReservationRepository` gains `TryReserveLinesAsync` (atomic multi-row CTE) + `ReleaseLinesAsync`; 9 cross-module contracts (`OrderPlacedV1`, `TrackingPushedV1`, `ReserveStockV1`, `ConfirmStockV1`, `ReleaseStockV1`, `StockReservedV1`, `StockReservationFailedV1`, `StockConfirmedV1`, `StockReleasedV1`); 3 Inventory consumers (ReserveStock / ConfirmStock / ReleaseStock). **K11 CTE concurrency defect caught + fixed** (predicate must live inside UPDATE, not in pre-check CTE — see `docs/solutions/2026-05-13-multi-row-cte-predicate-must-live-in-update.md`).
- ✅ U4 — `FulfillmentSaga` state machine (11 states) + EF saga repository on `saga_state` + **K12 per-tenant DbContext binding** via `TenantBindingSagaFilter<T>` (primary path) + `TenantAwareSagaDbContextFactory<FulfillmentSagaState>` (registered fallback); test-first cadence caught MT 8.x publish-DSL trap
- ✅ U5 — `IPickQueue` per-tenant `Channel<PickRequestV1>` (bounded 1000) + `PickWaveGeneratorService` (PeriodicTimer 30s tick; 15-min window batching by `(tenant_id, shipping_profile)`; round-robin picker via deterministic cursor); saga `Reserved` Then-handler writes PickRequest + chains TransitionTo(AwaitingPick)
- ✅ U6 — `confirm-pick` + `confirm-pack` (with weight-warning) + `confirm-ship` endpoints (501 stubs replaced); `IMockShippingProvider` (1-3s delay + 5% transient-fail + Polly v8 `ResiliencePipelineBuilder` retry); `ChannelTrackingConsumer` stub; AE5/AE6/AE7 covered (14 PackShipEndpointTests + 9 MockShippingProviderTests + 2 ChannelTrackingConsumerTests)
- ✅ U7 — `mark-pick-failed` endpoint + saga `CompensatingReservation` body (Path A atomic-fail empty-set short-circuit; Path B pick-fail publish `ReleaseStockV1`); Set-based dedup on `StockReleasedV1` via `ReleasedLineSkus` HashSet; `OrderCancelledConsumer` propagates saga terminal state to Order row (7 unit + 3 integration tests)
- ⚠️ U8 — `MultiTenantOutboundScaleGateTests` (2 tests, `Category=Load`): 2000 orders × 3 tenants. **Saga path bypassed** — operator-pipeline measurement only. Dev-laptop Shipped p99 247-332ms, Cancelled p99 112-131ms, fairness floor 0.918-0.979 (all well within R17 targets and ≥ 0.85 threshold). Real-saga-throughput-under-load is a Phase-2 production-CI measurement gap.
- ✅ U9 — Per-PR integration tests close U8's saga-bypass gap: `SagaHappyPathTests` (2) + `SagaCompensationFlowTests` (2) + `CrossModuleReservationFlowTests` (2 — real both-modules-one-DB round-trip) + `PickWaveBatchingFlowTests` (1). 7 tests in ~3s.
- ✅ U10 — [Sprint-3-redux sign-off](./docs/phase-gates/2026-05-13-sprint-3-redux-signoff.md); CHANGELOG entry; README + CLAUDE update; tag `v0.5.0-sprint-3-redux`

**Next implementation step**: cut a fresh branch from `v0.5.0-sprint-3-redux` and start **Phase-2 Sprint-4** (Channel Connections + webhook idempotency). K13 envelope-type → endpoint routing in `OutboxDispatcher` is a Phase-2 prerequisite for the W6 mechanical split.

**Sprint-3-redux deviations from plan file list**:
- **K11 CTE concurrency correction (U3)**: plan pseudocode's `will_succeed` pre-check CTE was unsafe under READ COMMITTED — caught by Sprint-1-redux's existing concurrent-oversell test. Corrected pattern (predicate in UPDATE + `all_succeeded` NOT-EXISTS gate) shipped + documented as institutional learning. Plan K11 prose updated.
- **U8 saga bypass**: scale gate's auto-driver writes `Order.status` directly instead of routing through the saga's OrderPlacedV1 → ReserveStockV1 → StockReservedV1 → AwaitingPick chain. Measures HTTP+DB-write throughput, not full saga throughput. Saga correctness gated by U4/U7/U9 integration tests; full-saga-under-load deferred to production CI.
- **U8 mock-carrier delay shortened** (5-20ms vs production 1-3s) for bounded scale-gate wall-time; real-delay path covered by `MockShippingProviderTests` at unit scale.
- **U8 warm-up phase + `NpgsqlConnection.ClearAllPools()` between tests** for repeatable runs (Postgres `max_connections=100` cap with 3 tenants).
- **U9 PickWaveBatchingFlowTests seeds PickRequests directly** instead of driving 50 sagas (45s → 383ms on dev hardware); AE4 invariant unchanged.
- **U1 saga_state inlined extension**: U4's per-state context columns (`tenant_id`, `shipping_profile`, `line_count`, `reserved_line_skus`, etc.) added inline to the U1 migration (not a follow-on migration). Safe because migration hadn't tagged or applied anywhere yet.
- **MT 8.x publish DSL**: `Publish(ctx => new T(...))` works inside `Initially`; `PublishAsync(ctx.Init<T>(new {...}))` silently fails. Caught by test-first cadence in U4.
- **K13 W6 deferral**: `OutboxDispatcher.Publish`-for-commands accepted as Sprint-3-redux trade-off (modular monolith). W6 split needs envelope-type → endpoint routing — Phase-2 prerequisite tracked.

---

**Sprint-2.5 history** (kept for context; tag `v0.4.1-sprint-2.5`). Sign-off: [`docs/phase-gates/2026-05-13-sprint-2.5-signoff.md`](./docs/phase-gates/2026-05-13-sprint-2.5-signoff.md). Closes the Sprint-2-redux U9 deferral: per-module outbox table-name prefix (`inbound_outbox_messages` / `inventory_outbox_messages`) unblocks single-physical-tenant-DB cross-module flow. Two cross-module flow integration tests landed against shared Testcontainers Postgres. Surfaced + fixed a latent JSON-options bug in the dispatcher pipeline (camelCase serialise vs case-sensitive deserialise) via `OutboxJsonOptions.Default` in SharedKernel.

**Sprint-2.5 progress** (as of 2026-05-13):
- ✅ U1 — Inbound `outbox_messages` → `inbound_outbox_messages` (entity config + migration + smoke test)
- ✅ U2 — Inventory `outbox_messages` → `inventory_outbox_messages` (entity config + Phase-0-redux U8 migration edited in-place + smoke test + raw-SQL test fixtures)
- ✅ U3 — `InboundToInventoryFlowTests` (2 tests) lands against single shared tenant DB; `ShopFlow.SharedKernel.Infrastructure.OutboxJsonOptions.Default` centralises JSON options across 4 call sites (OutboxInterceptor, MultiplexedOutboxDispatcher, InboundOutbox, ReservationRepository)
- ✅ U4 — [Sprint-2.5 sign-off](./docs/phase-gates/2026-05-13-sprint-2.5-signoff.md); CHANGELOG + tag `v0.4.1-sprint-2.5`

---

**Sprint-2-redux history** (kept for context; tag `v0.4.0-sprint-2-redux`). Sign-off: [`docs/phase-gates/2026-05-13-sprint-2-redux-signoff.md`](./docs/phase-gates/2026-05-13-sprint-2-redux-signoff.md). The Inbound module + Inventory bin/zone schema extension + MassTransit RabbitMQ transport flip (W6 → W4) ship together; the first cross-module write flow (Inbound → Inventory via `ShopFlow.Contracts.Inbound.InboundConfirmedV1`) is wired end-to-end at the service + consumer level.

**Sprint-2-redux progress** (as of 2026-05-13):
- ✅ U1 — Inbound module quartet scaffold (Domain/Application/Infrastructure/Api) + 6-table `InitialInboundSchema` migration
- ✅ U2 — `PurchaseOrder` aggregate state machine + repository + 18 Domain unit tests + 4 integration tests
- ✅ U3 — `Receiving` + `ReconciliationTicket` aggregates + `ConfirmReceivingLineService` orchestrator + 6 integration tests
- ✅ U4 — Inventory schema extension: `zones`, `bins`, `stock_item_bins`, `home_zone_id`, `inbound_dedup` tables + entity configs + ports + repos
- ✅ U5 — Bin-aware `StockItemRepository.AdjustAtBinAsync` (auto-create + upsert + audit in one ReadCommitted transaction) + `PutAwaySuggestionService` (top-K ranking) + put-away controller + 8 integration tests
- ✅ U6 — `ShopFlow.Contracts.Inbound.InboundConfirmedV1` cross-module event + `IInboundOutbox` explicit-write port + `InboundConfirmedConsumer` (Inventory side, idempotent via `inbound_dedup`) + 3 consumer integration tests
- ✅ U7 — MassTransit `MessageBusTransport` enum + config switch + `AddShopFlowDefaults` wired in Inbound.Api + Inventory.Api Program.cs + ADR-0002 W6 → W4 postscript
- ✅ U8 — `PurchaseOrdersController` thin endpoints (POST/GET/PATCH for PO + POST /receive for the ConfirmReceivingLineService call)
- ⚠️ U9 — Single-tenant-DB cross-module flow test **deferred**; surfaced an architecture finding (both modules' migrations create `outbox_messages` in `public` schema → collision when sharing a tenant DB). Documented as [docs/solutions/2026-05-13-cross-module-outbox-table-name-collision.md](./docs/solutions/2026-05-13-cross-module-outbox-table-name-collision.md); Sprint-2.5 candidate
- ✅ U10 — [Sprint-2-redux sign-off](./docs/phase-gates/2026-05-13-sprint-2-redux-signoff.md); CHANGELOG entry; README + CLAUDE current-stage update; tag `v0.4.0-sprint-2-redux`

**Next implementation step**: cut a fresh branch from `v0.4.0-sprint-2-redux` and start either:
- **Sprint-2.5** (the cross-module outbox table-name rename) — small focused unit to close the U9 gap, OR
- **Sprint-3-redux** (W5 Outbound + fulfillment saga) — original 12-week roadmap; can run in parallel with the Sprint-2.5 fix since Outbound doesn't depend on Inbound→Inventory's flow being physically-shared-DB-safe yet

**Sprint-2-redux deviations from plan file list**:
- **U6 — Domain event path swapped for explicit `IInboundOutbox`**: making `InboundConfirmedV1` implement `IDomainEvent` would create a SharedKernel → Contracts cycle. Pivoted to the explicit-outbox-write pattern matching Sprint-1-redux's `ReservationRepository.AppendOutbox`. `InboundLineConfirmedDomainEvent` deleted; Receiving aggregate no longer raises events.
- **U8 — MediatR command/handler wrapper deferred**: controllers call `ConfirmReceivingLineService` POCO directly. MediatR pipeline (logging/tracing/validation) is wired by `AddShopFlowDefaults` but no commands defined. Future sprint can layer command/handler on top trivially.
- **U8 — HTTP `WebApplicationFactory` tests skipped**: covered by U2/U3/U6 integration tests at the service + repo + consumer level.
- **U9 — Cross-module flow test deferred** (architecture finding above).
- **U4 — Identity-column annotation fix**: Npgsql's `IdentityByDefaultColumn` annotation needs the typed enum, not a plain string — discovered when zone insert tripped NOT NULL. Documented inline in the migration; carry-forward rule for future identity columns.

---

**Sprint-1-redux history** (kept for context; tag `v0.3.0-sprint-1-redux`). Sign-off: [`docs/phase-gates/2026-05-12-sprint-1-redux-signoff.md`](./docs/phase-gates/2026-05-12-sprint-1-redux-signoff.md). The reservation ledger ships against the DB-per-tenant foundation with the conditional-CTE INSERT at READ COMMITTED — the v3.0 correction over the v2.0 SERIALIZABLE shape.

**Sprint-1-redux progress** (as of 2026-05-12):
- ✅ U1 — `ReservationRepository.TryReserveAsync` — conditional-CTE INSERT at ReadCommitted + 23505 idempotency + StockReservedEvent outbox
- ✅ U2 — `FindByOrderIdAsync` + `ConfirmAsync` (NOT_FOUND/ALREADY_CONFIRMED/INVALID_STATE codes) + `ReleaseAsync` + `ReleaseExpiredAsync` multi-CTE batched UPDATE; Reservation + StockItem domain methods filled in
- ✅ U3 — Multiplexed `ReservationExpiryWorker` BackgroundService; `InventoryOptions` config surface (`ExpiryPollIntervalSeconds`, `ExpiryBatchSize`, `DefaultReservationTtlMinutes`)
- ✅ U4 — `ShopFlow.PropertyTests` project: `PostgresPropertyFixture` + `NotImplementedReservationRepository` adapter + 5 FsCheck properties (HappyPathConcurrency / StrictCapacity / Idempotency / ExpiryReleasesActiveRows / InvariantHoldsForAnyOperationSequence)
- ✅ U5 — `MultiTenantScaleGateTests` (5×1000 fairness floor gate + 1-stock-N-callers oversell variant) + `TenantHarness` + `FairnessCalculator`
- ✅ U6 — [Sprint-1-redux sign-off](./docs/phase-gates/2026-05-12-sprint-1-redux-signoff.md); [docs/solutions/2026-05-12-readcommitted-conditional-cte-correctness.md](./docs/solutions/2026-05-12-readcommitted-conditional-cte-correctness.md); CHANGELOG entry; tag `v0.3.0-sprint-1-redux`

**Next implementation step**: cut a fresh branch from `v0.3.0-sprint-1-redux` and start Sprint-2-redux (Inbound module W4). Plan still to be written. Read-back surface for `IReservationRepository` (`GetActiveSumAsync` / `GetConfirmedSumAsync`) carries forward as a Sprint-2-redux side-quest because Inbound's GRN reconciliation needs the same shape; once landed, Property 5 in the FsCheck suite swaps its raw-SQL ledger read for the port call.

**Sprint-1-redux deviations from plan file list**:
- **U4 — Property "zero test-body edits" relaxed**: R3 expected the archived Sprint-1 property bodies to flip green with only fixture wiring changes. The archived bodies target the pre-redux port shape (`Result<Guid>`, `Guid orderId`, explicit `tenantId` parameter). U8 pivoted the port to `Result<Reservation>` / `string orderId` / no tenant parameter. The 5 properties are re-derived with the same names + same pinned seed against the new port shape; the call sites changed, the invariants did not.
- **U4 — Property 5 read-back surface gap remains open**: the canonical `GetActiveSumAsync` / `GetConfirmedSumAsync` read-back is not declared on `IReservationRepository`. Property 5 reads the ledger directly via raw SQL as a stop-gap. Sprint-2-redux closes when Inbound also needs the read-back surface.
- **U5 — Scale-gate runtime deferred**: code-complete, tagged `Category=Integration` + `Category=Load`. Wall-time measurement on this dev machine deferred because Docker Desktop is installed but the daemon is not running (same blocker as Phase-0-redux U10). CI captures the number once first nightly run completes.
- **U1+U2 — Direct repository wiring**: the repository takes `InventoryDbContext` by DI from the U8-shipped scoped registration rather than via `IDbContextFactory<InventoryDbContext>`. Functionally equivalent for the request-scoped path (the DbContext is built per scope using `IRequestContext.DbConnectionString`); ShopFlow0003 exempt because construction lives in a service registration lambda. Multiplexed worker (U3) uses `IServiceScopeFactory.CreateAsyncScope` + `RequestContext.Bind` to flow tenant context. Open-generic factory plumbing remains in `AddShopFlowDefaults` for any future per-message dispatcher path.

---

**Phase-0-redux history** (as of 2026-05-12 — kept for resume context):

**Phase-0-redux progress** (as of 2026-05-12 session 3):
- ✅ U1 — Canon verification + AGENTS.md numbering fix (commit `0111ee7`)
- ✅ U2 — Repo skeleton, sln, props, `global.json` pinning .NET 9 (commit `a26f507`)
- ✅ U3 — Channel test fixtures cherry-picked (commit `31c8a07`)
- ✅ U4 — SharedKernel + 4 analyzers (ShopFlow0001-0004) + 8 passing unit tests; build clean (commit `a9a8c62`)
- ✅ U5 — ControlPlane quartet (Domain/Application/Infrastructure/Migrations) + Tenant aggregate + initial catalog migration + 16 state-machine tests (commit `6307242`)
- ✅ U6 — `shopflow-migrate` CLI: provision/apply/archive/restore/status + 35 unit tests (commit `ee616df`)
- ✅ U7 — Aspire AppHost (Postgres + PgBouncer + Redis + RabbitMQ + observability) + chained provisioning of catalog/dev1/dev2 + production handoff via `infrastructure/docker-compose.yml`
- ✅ U8 — Inventory module (Domain entities + value objects + 4 domain events; 3 Application ports; Infrastructure DbContext + 4 entity configs + repo skeletons throwing NIE; ReservationExpiryWorker hosted-service stub; InitialInventorySchema migration with mandatory `[Migration]`+`[DbContext]` attributes; Api 501 skeleton) + 16 Domain unit tests (commit `c9f642d`)
- ✅ U9 — Module shape replicated (Inbound/Outbound/Channel quartets, Analytics triplet, Gateway YARP scaffold); per-module AGENTS.md deltas; 5 smoke test projects locking the shape in CI (commit `2a9cd41`)
- ✅ U10 — CI workflows (`.github/workflows/ci.yml` + `chaos-nightly.yml`); ShopFlow0001-0004 analyzers promoted Warning → Error; `MigrationSmokeTests` (2) + `CrossTenantRoutingTests` (5) against Testcontainers Postgres; `shopflow-gate phase-0-redux` operational CLI; [phase-0-redux sign-off](./docs/phase-gates/2026-05-12-phase-0-redux-signoff.md); README + CHANGELOG updates; tag `v0.2.0-phase-0-redux`

**Phase-0-redux is complete.** Tag: `v0.2.0-phase-0-redux`. Sign-off: [`docs/phase-gates/2026-05-12-phase-0-redux-signoff.md`](./docs/phase-gates/2026-05-12-phase-0-redux-signoff.md).

**U10 deviations from plan file list**:
- `shopflow-gate` shipped as a NEW project under `tools/shopflow-gate/` (the plan said "carries forward" but the v2.0 implementation wasn't on this branch — `task gate` referenced an absent csproj). Minimal CLI implements `gate phase-0-redux` with 4 checks (catalog reachable, catalog migrated, all tenants Ready, PgBouncer reachable). The richer in-cluster checks (provisioning latency p99, RabbitMQ live, observability stack live) are Phase-2 deliverables; the CLI shape is stable so adding a check is one method.
- `MigrationSmokeTests` parameterizes over the two known DbContexts directly (ControlPlane + Inventory) rather than reflection-discovering all DbContexts. The reflection version is a Sprint-1-redux improvement — cheap when a third DbContext lands; not load-bearing for U10's "guards the v2.0 silent-no-op defect" goal.
- `CrossTenantRoutingTests` exercises the middleware directly with a synthetic `DefaultHttpContext` and a `FakeTenantCatalog` rather than spinning a full `TestServer`. The contract under test is the slug→TenantInfo→DbConnectionString binding plus a real database read through that binding — sufficient to catch any wrong-DB routing bug at the level that matters. Full TestServer with the entire request pipeline is a Sprint-1-redux upgrade.
- Aspire cold-start + provisioning latency p99 measurements **deferred** in the sign-off — Docker daemon isn't running on this dev machine. Documented as a one-line table update in the sign-off doc once a Docker-enabled session lands.
- CSharpier formatting cleanup **deferred** to a follow-up commit — 23 files inherited from U4-U6 don't match CSharpier output (mostly LF/CRLF + line-fold disagreements). CI's `csharpier --check` step will block on first run; one cleanup commit fixes them.

**U9 deviations from plan file list**:
- Smoke tests for the 4 module shapes are `[Fact]`-level checks that the marker class exposes the expected `ModuleName` string. Gateway smoke test inspects the rendered `appsettings.json` for the 5 expected route names; this is a structural assertion, not a YARP integration test (the full integration suite lives in `tests/ShopFlow.<Module>.IntegrationTests/` per AGENTS.md §11.81 once real handlers land).
- Gateway routes use `http://<module>:8080` Docker-DNS-style destinations in `appsettings.json` — that's the W6 split-host shape. In the W1-W5 modular-monolith stance the modules all run as in-process controllers under one Aspire host, so the gateway routes are aspirational. U10 wires the AppHost to register each module's API as an Aspire resource so the gateway upstream addresses resolve.
- Per-module AGENTS.md files are ≤ 50 lines each per root rule 82; the U9 stub-state notes are intentionally terse so they're easy to delete when Phase-1+ replaces them.

**U8 deviations from plan file list**:
- `StockItem` inherits `BaseEntity` (not `AggregateRoot`) because the inherited `byte[] RowVersion` on `AggregateRoot` doesn't match the Postgres `xid` shape Tech Design v3.0 §4.2 wants; `StockItem` declares its own `uint RowVersion`. EF `Ignore`s the inherited Guid `Id`; `HasKey(s => s.Sku)` is the natural PK. The domain-event buffer from `BaseEntity` survives.
- A 4th test project landed (`tests/ShopFlow.Inventory.UnitTests/` — 16 tests for Sku/Quantity/Reservation.Create + Sprint-1-redux NIE detection). The plan didn't strictly require it, but the Domain primitives have real behavior that's cheap to lock down here; Sprint-1-redux behavior tests live in a separate Integration/Property project later.
- Inventory.Api `Program.cs` does NOT call `services.AddShopFlowDefaults(...)` yet — that composition entry point lands in U9-U10 along with the kernel-wide composition root. The Api project compiles and the controller returns 501 either way; the wiring gap is documented in the Program.cs comment and is a U10 concern.

**U7 deviations from plan file list**: AppHost csproj requires `<Sdk Name="Aspire.AppHost.Sdk" Version="13.3.0" />` (in addition to the `Aspire.Hosting.AppHost` package) — Aspire 13.x's MSBuild SDK is what tells .NET 9's `ImportWorkloads` target the workload is satisfied via NuGet; without it `dotnet build` raises NETSDK1147. CPM bumped `Microsoft.Extensions.Hosting` from 9.0.0 → 10.0.7 (Aspire 13.3.0 transitively requires the 10.x floor; cross-targets net9.0 cleanly). PgBouncer config is generated at AppHost startup from `infrastructure/pgbouncer/pgbouncer.ini.template` and bind-mounted into the bitnami container; the same template ships unmodified to the prod compose manifest. Mock channel servers are reserved as commented placeholders (Phase-2 Sprint-4 deliverable). The `task up` cold-start time + `GET /api/health` tenant-routing scenarios are deferred to U10 sign-off — Inventory.Api lands in U8.

**U5 deviation from plan file list**: `ITenantCatalog` port stays in `ShopFlow.SharedKernel.Application.Ports` (where U4 placed it) rather than being re-created in `ControlPlane.Application.Ports` — the SharedKernel routing middleware and outbox dispatcher consume the port and cannot take a backward dep on ControlPlane. `TenantStatus` enum relocated from `SharedKernel.Application.Ports` to `SharedKernel.Domain` (pure value type) so `ControlPlane.Domain.Tenant` and `Application.Ports.TenantInfo` share one enum without violating layering. The Migrations csproj omits `Microsoft.EntityFrameworkCore.Design` (NU1608 vs CPM-pinned CodeAnalysis 4.11.0); the runtime apply path used by `shopflow-migrate` and U10's smoke test does not need it.

**Session-1 decisions captured** (apply when resuming):
- **D1 PgBouncer pool sizing (U7)**: `pool_mode=transaction`, `default_pool_size=20`, `max_db_connections=20`, `min_pool_size=2`, `reserve_pool_size=5`. Postgres `max_connections=500` in dev, document `1000` for prod.
- **D2 Catalog cache (U5)**: 5 min TTL, LRU size 1000. Synchronous eviction on write paths (provision-complete, archive-start).
- **D3 Migration smoke test assertions (U8/U10)**: load-bearing assertion is `__ef_migrations_history` row count ≥ 1 after `MigrateAsync()`. Per-module named-table existence + named PK / UNIQUE constraint existence. No `pg_dump --schema-only` diff.
- **D4 Routing middleware (already implemented in U4)**: header > JWT > subdomain priority; 2+ source conflict → 403 + audit row; 10 concrete scenarios documented in `TenantRoutingMiddleware.cs`.

**Build/test invariants for resume**:
- `dotnet build` → 0 warnings, 0 errors across 41 projects (29 src + 11 test + 1 gate tool) — Inventory.IntegrationTests + PropertyTests added in Sprint-1-redux
- `dotnet test --filter "Category!=Integration"` → 92 passed (8 SharedKernel + 16 ControlPlane + 28 Inventory Domain + 35 Migrate + 5 module-shape smoke) — Sprint-1-redux added 12 Reservation/StockItem state-machine tests
- `dotnet test --filter "Category=Integration"` → ~26 tests (7 SharedKernel + 14 Inventory + 5 PropertyTests). Needs Docker; runs in CI on every PR.
- `dotnet test --filter "Category=Load"` → 2 tests in `MultiTenantScaleGateTests`. Needs Docker; nightly + on-demand only.
- .NET 9.0.305 SDK pinned via `global.json`; Aspire AppHost MSBuild SDK 13.3.0 referenced by `ShopFlow.AppHost.csproj`
- Pre-existing csharpier drift on 23 files (mix of LF/CRLF inheritance + line-fold disagreements) carried over from U4-U6 commits; Sprint-1-redux added a handful more files that may also drift. Husky pre-commit hook is not yet installed on this dev machine (`.husky/_/` absent), so commits do not enforce csharpier locally. CI's `csharpier --check` step will block on first run; one cleanup commit fixes them.

**Always read `docs/CHANGELOG.md` first** to understand what supersedes what. Then `docs/solutions/` for accumulated learnings (re-discovery prevention).

To resume implementation, the next concrete entry point is Sprint-2-redux (Inbound module W4) — plan still to be written. To deepen a design decision, run `/compound-engineering:ce-brainstorm` or `/compound-engineering:ce-plan`.
